# Dependencies
node_modules/

# Build outputs
dist/
dist-ssr/
*.tsbuildinfo

# Environment variables
.env
.env.local
.env.*.local
.env.development
.env.production

# Logs
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Test coverage
coverage/
coverage.json
.nyc_output/

# IDE / Editors
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json
.idea/
*.sublime-project
*.sublime-workspace
*.swp
*.swo
*.swn
*~

# OS files
.DS_Store
.DS_Store?
._*
Thumbs.db
ehthumbs.db
Desktop.ini

# Local tool configs (user-specific)
.serena/
*.local

# Vite
vite.config.ts.timestamp-*

# Optional: Debug
*.map.local

# Temporary files
tmp/
temp/
*.tmp
*.temp
*.backup
*.bak


.mcp.json
.mcp.local.json
.mcp_servers.json

# Tool artifacts (Playwright MCP, RTK, generated images)
.playwright-mcp/
.rtk/
generated_imgs/

# Agent worktrees — real git repos; committing them as gitlinks breaks actions/checkout
# with "No url found for submodule path" since there is no .gitmodules.
.claude/

# not redistributable sizing workbooks — never commit. Derived data lives in
# src/data/powerscale*.json, regenerated by scripts/build-powerscale-catalog.mjs
*.xlsm
*.xlsb
# jsPDF's `save` is an own property per instance and cannot be stubbed, so the export smoke
# test writes a real PDF and removes it in afterAll; an interrupted run would leave it.
Raidy_Export_*.pdf
