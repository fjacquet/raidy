---
phase: 03-security-hardening
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - public/_headers
  - vercel.json
  - public/index.html
  - docs/SECURITY.md
  - .github/workflows/ci.yml
autonomous: true

must_haves:
  truths:
    - "Developer can deploy to Netlify with CSP headers automatically applied"
    - "Developer can deploy to Vercel with CSP headers automatically applied"
    - "GitHub Pages deployment has CSP meta tag (with documented limitations)"
    - "npm audit shows zero high/critical vulnerabilities"
    - "Snyk scan passes with zero high/critical issues"
  artifacts:
    - path: "public/_headers"
      provides: "Netlify CSP configuration"
      contains: "Content-Security-Policy"
    - path: "vercel.json"
      provides: "Vercel security headers"
      contains: "Content-Security-Policy"
    - path: "public/index.html"
      provides: "CSP meta tag for GitHub Pages"
      contains: "http-equiv=\"Content-Security-Policy\""
    - path: "docs/SECURITY.md"
      provides: "Security workflow documentation"
      min_lines: 50
    - path: ".github/workflows/ci.yml"
      provides: "Automated security scanning"
      contains: "npm audit"
  key_links:
    - from: "deployment platform"
      to: "CSP config file"
      via: "reads _headers or vercel.json"
      pattern: "Content-Security-Policy.*default-src"
    - from: "CI/CD"
      to: "security scans"
      via: "npm audit && snyk test"
      pattern: "audit.*snyk"
---

<objective>
Configure Content Security Policy headers for all deployment platforms and establish automated security scanning workflow.

Purpose: CSP headers prevent XSS attacks by restricting script/style sources. GitHub Pages has CSP limitations (meta tag only), so document this and recommend Netlify/Vercel for production. Automated security scans catch vulnerabilities before deployment.

Output: CSP configuration for Netlify/Vercel/GitHub Pages + security scanning in CI/CD, passing SEC-06 (CSP headers) and SEC-09 (dependency scan) requirements.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-security-hardening/03-RESEARCH.md
@.planning/codebase/STACK.md

# Deployment configuration
@vite.config.ts

# Research references
# RESEARCH.md lines 103-145 (CSP configuration patterns)
# RESEARCH.md lines 273-278 (GitHub Pages CSP limitations)
</context>

<tasks>

<task type="auto">
  <name>Configure CSP headers for all deployment platforms</name>
  <files>public/_headers, vercel.json, public/index.html</files>
  <action>
**Step 1: Create public/_headers for Netlify**

```
/*
  Content-Security-Policy: default-src 'self'; script-src 'self' 'unsafe-inline'; style-src 'self' 'unsafe-inline'; img-src 'self' data: https:; font-src 'self' data:; connect-src 'self'; frame-ancestors 'none'; base-uri 'self'; form-action 'self'
  X-Content-Type-Options: nosniff
  X-Frame-Options: DENY
  X-XSS-Protection: 1; mode=block
  Referrer-Policy: strict-origin-when-cross-origin
  Permissions-Policy: geolocation=(), microphone=(), camera=()
```

**CSP directive explanation:**
- `default-src 'self'`: Only load resources from same origin
- `script-src 'self' 'unsafe-inline'`: Allow inline scripts (required for Vite/React)
- `style-src 'self' 'unsafe-inline'`: Allow inline styles (required for Tailwind)
- `img-src 'self' data: https:`: Allow images from same origin, data URIs, HTTPS
- `font-src 'self' data:`: Allow fonts from same origin and data URIs
- `connect-src 'self'`: API calls only to same origin
- `frame-ancestors 'none'`: Prevent embedding in iframes
- `base-uri 'self'`: Restrict base tag to same origin
- `form-action 'self'`: Form submissions only to same origin

**Why 'unsafe-inline':** Tailwind CSS generates utility classes as inline styles; React may use inline event handlers. This is a reasonable tradeoff for a static SPA. Long-term: use nonces or hashes for stricter CSP.

**Step 2: Create vercel.json for Vercel**

```json
{
  "headers": [
    {
      "source": "/(.*)",
      "headers": [
        {
          "key": "Content-Security-Policy",
          "value": "default-src 'self'; script-src 'self' 'unsafe-inline'; style-src 'self' 'unsafe-inline'; img-src 'self' data: https:; font-src 'self' data:; connect-src 'self'; frame-ancestors 'none'; base-uri 'self'; form-action 'self'"
        },
        {
          "key": "X-Content-Type-Options",
          "value": "nosniff"
        },
        {
          "key": "X-Frame-Options",
          "value": "DENY"
        },
        {
          "key": "X-XSS-Protection",
          "value": "1; mode=block"
        },
        {
          "key": "Referrer-Policy",
          "value": "strict-origin-when-cross-origin"
        },
        {
          "key": "Permissions-Policy",
          "value": "geolocation=(), microphone=(), camera=()"
        }
      ]
    }
  ]
}
```

**Step 3: Add CSP meta tag to public/index.html**

Find the `<head>` section and add:

```html
<!-- Content Security Policy for GitHub Pages (limited - no frame-ancestors support) -->
<meta http-equiv="Content-Security-Policy"
      content="default-src 'self'; script-src 'self' 'unsafe-inline'; style-src 'self' 'unsafe-inline'; img-src 'self' data: https:; font-src 'self' data:; connect-src 'self'">
```

**GitHub Pages limitation:** Meta tag CSP cannot enforce `frame-ancestors`, `report-uri`, or other directives. Document this in SECURITY.md.

**Note:** If public/index.html doesn't exist (Vite may generate it), create it in public/ directory with basic HTML structure.

Reference RESEARCH.md lines 103-145 for CSP configuration patterns.
  </action>
  <verify>Files exist with correct CSP directives; CSP syntax validated</verify>
  <done>CSP headers configured for Netlify (_headers), Vercel (vercel.json), and GitHub Pages (meta tag)</done>
</task>

<task type="auto">
  <name>Run and fix dependency security scans</name>
  <files>.github/workflows/ci.yml</files>
  <action>
**Step 1: Run security scans locally**

```bash
# Run npm audit
npm audit --production

# Run Snyk scan (if authenticated)
npm run security

# Check for high/critical vulnerabilities
npm audit --audit-level=high
```

**Step 2: Fix any found vulnerabilities**

If high/critical vulnerabilities found:
1. `npm audit fix` (applies automatic fixes)
2. Review breaking changes in updated packages
3. If no automatic fix: `npm audit fix --force` (may break compatibility)
4. If still unresolved: Document workaround or acceptance in SECURITY.md

**Step 3: Add security scanning to CI/CD**

Modify `.github/workflows/ci.yml` to add security job:

```yaml
security:
  runs-on: ubuntu-latest
  steps:
    - uses: actions/checkout@v4
    - uses: actions/setup-node@v4
      with:
        node-version: '20'
    - run: npm ci
    - name: Run npm audit
      run: npm audit --audit-level=high
    - name: Run Snyk security scan
      run: npx snyk test --severity-threshold=high
      env:
        SNYK_TOKEN: ${{ secrets.SNYK_TOKEN }}
      continue-on-error: false
```

**Note:** Snyk requires SNYK_TOKEN secret in GitHub repository settings. If not available, document setup in SECURITY.md.

**Why production-only audit:** Dev dependencies (testing, build tools) don't ship to users. Only production dependencies pose runtime security risk.

**Expected state:**
- jsPDF 4.0.0 (CVE-2025-68428 fixed) ✓
- React 19.2.0 (recent, should be clean) ✓
- Zustand, lz-string, others (check for known issues)

If vulnerabilities found, apply fixes and rerun tests to verify no breakage.
  </action>
  <verify>npm audit --audit-level=high passes with zero vulnerabilities; CI workflow includes security scanning</verify>
  <done>Dependency vulnerabilities fixed; automated security scanning in CI/CD</done>
</task>

<task type="auto">
  <name>Document security configuration and workflow</name>
  <files>docs/SECURITY.md</files>
  <action>
**Create docs/SECURITY.md:**

```markdown
# Security Configuration

**Project:** Raidy Storage Simulator
**Last Updated:** 2026-01-18

## Overview

Raidy implements security hardening for client-side SPA applications:
- URL state validation prevents malicious hash injection
- Input sanitization protects PDF export
- Error boundaries prevent information leakage
- Content Security Policy headers mitigate XSS attacks
- Automated dependency scanning catches vulnerabilities

## Content Security Policy (CSP)

### Deployment Platforms

**Netlify (Recommended for Production)**
- Configuration: `public/_headers`
- Headers: CSP, X-Frame-Options, X-Content-Type-Options, Referrer-Policy
- Full CSP support including frame-ancestors directive

**Vercel**
- Configuration: `vercel.json`
- Headers: Same as Netlify
- Full CSP support

**GitHub Pages (Limited)**
- Configuration: `public/index.html` meta tag
- Limitation: Meta tag CSP does NOT support:
  - `frame-ancestors` directive (clickjacking protection missing)
  - `report-uri` directive (violation reporting unavailable)
- Recommendation: Use Netlify or Vercel for production deployments

### CSP Directives Explained

- `default-src 'self'`: Only load resources from same origin
- `script-src 'self' 'unsafe-inline'`: Allow inline scripts (required for React)
- `style-src 'self' 'unsafe-inline'`: Allow inline styles (required for Tailwind)
- `frame-ancestors 'none'`: Prevent iframe embedding (not supported in meta tag)

**Why 'unsafe-inline':**
Tailwind CSS utility classes and React event handlers require inline styles/scripts. This is standard for React SPAs. Future enhancement: use nonces or hashes for stricter CSP.

### Testing CSP

1. Deploy to Netlify/Vercel test environment
2. Open browser DevTools Console
3. Check for CSP violation errors
4. Test that app functions correctly (no blocked resources)

## Dependency Security

### Scanning Tools

1. **npm audit** (built-in)
   - Scans dependencies against npm security advisory database
   - Run: `npm audit --audit-level=high`
   - Automatic fixes: `npm audit fix`

2. **Snyk** (third-party)
   - More comprehensive vulnerability database
   - Run: `npm run security` (requires authentication)
   - Setup: `npm run security:code` for code analysis

### CI/CD Integration

Security scans run automatically on every push:
- GitHub Actions workflow: `.github/workflows/ci.yml`
- Fails build if high/critical vulnerabilities found
- Snyk requires `SNYK_TOKEN` secret in repository settings

### Known Vulnerabilities (Current)

- jsPDF 4.0.0: CVE-2025-68428 fixed ✓
- All dependencies: Clean scan as of 2026-01-18 ✓

### Fixing Vulnerabilities

When scan finds issues:
1. `npm audit fix` (try automatic fix first)
2. Review changelog for breaking changes
3. Run `npm test` to verify no breakage
4. If no fix available: document accepted risk or workaround

## Input Validation

### URL State Validation

**Protection:** Malicious URL hash cannot inject invalid values (negative counts, NaN, wrong enums)

**Implementation:**
- File: `src/utils/schemas.ts`
- Library: Zod 3.24+
- Validation after LZ-string decompression in `src/store/urlStorage.ts`

**Test:**
Load URL: `http://localhost:5173/#config=<malicious_payload>`
Expected: User sees notification "Configuration link is invalid"

### PDF Export Sanitization

**Protection:** User-controlled text fields (projectName) cannot inject XSS vectors

**Implementation:**
- File: `src/utils/exportPdf.ts`
- Library: DOMPurify 3.3+
- Sanitization before jsPDF rendering

**Test:**
Export PDF with projectName: `<script>alert('xss')</script>Test`
Expected: PDF shows "Test" without script tags

### Validation Enforcement

**Protection:** Invalid configurations (RAID5 with 2 drives) cannot trigger calculations

**Implementation:**
- File: `src/utils/validators.ts`
- Function: `hasBlockingErrors()` prevents calculation execution
- Error boundary: `src/components/ErrorBoundary.tsx` catches crashes

## Responsible Disclosure

If you discover a security vulnerability:
1. **Do NOT** create public GitHub issue
2. Email: security@example.com (update with actual contact)
3. Include: reproduction steps, impact assessment, suggested fix
4. Expected response: 48 hours

## Security Checklist for Deployment

- [ ] Run `npm audit --audit-level=high` (zero vulnerabilities)
- [ ] Run `npm run security` (Snyk scan passes)
- [ ] Verify CSP headers in deployment platform
- [ ] Test URL validation with malicious inputs
- [ ] Test PDF export with XSS vectors
- [ ] Verify error boundary shows fallback (not stack traces)
- [ ] Check that invalid configs are blocked from calculation

## References

- OWASP Input Validation: https://cheatsheetseries.owasp.org/cheatsheets/Input_Validation_Cheat_Sheet.html
- CSP Documentation: https://developer.mozilla.org/en-US/docs/Web/HTTP/CSP
- React Security Best Practices: https://snyk.io/blog/10-react-security-best-practices/
```

Update contact email and any project-specific details.
  </action>
  <verify>docs/SECURITY.md exists with comprehensive security documentation</verify>
  <done>Security workflow documented with CSP configuration, scanning procedures, and deployment checklist</done>
</task>

</tasks>

<verification>
**Manual verification:**
1. Deploy to Netlify test: Verify CSP headers present (DevTools Network tab)
2. Deploy to Vercel test: Verify CSP headers present
3. Run locally with GitHub Pages meta tag: Verify app functions
4. Run `npm audit --audit-level=high` (expect zero vulnerabilities)
5. Run `npm run security` (Snyk scan passes)

**Automated verification:**
```bash
npm audit --audit-level=high
npm run security (if authenticated)
```

**Success indicators:**
- CSP config files exist with correct directives
- Security scans pass with zero high/critical issues
- CI/CD includes security scanning job
- Documentation complete
</verification>

<success_criteria>
- [ ] public/_headers created with Netlify CSP configuration
- [ ] vercel.json created with Vercel security headers
- [ ] public/index.html includes CSP meta tag (with limitations documented)
- [ ] npm audit --audit-level=high passes with zero vulnerabilities
- [ ] Snyk scan passes (or documented if not authenticated)
- [ ] .github/workflows/ci.yml includes security scanning job
- [ ] docs/SECURITY.md documents CSP configuration, scanning procedures, deployment checklist
- [ ] All deployment platforms have security headers configured
- [ ] SEC-06 (CSP headers) and SEC-09 (dependency scan) requirements met
</success_criteria>

<output>
After completion, create `.planning/phases/03-security-hardening/03-04-SUMMARY.md` documenting:
- CSP configuration for all deployment platforms
- GitHub Pages CSP limitations and recommendations
- Dependency security scan results and fixes (if any)
- Security workflow established in CI/CD
- Key decision: 'unsafe-inline' CSP tradeoff for React/Tailwind compatibility
</output>
