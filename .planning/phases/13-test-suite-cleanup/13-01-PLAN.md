---
phase: 13-test-suite-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/utils/units.spec.ts
  - tests/utils/connectivityConstraints.spec.ts
autonomous: true
requirements:
  - DELL-11
  - DELL-12
must_haves:
  truths:
    - "npm run test:coverage passes the 75% statement/line/function/branch thresholds"
    - "tests/fixtures/dell-vectors.ts contains ME5224 ADAPT 12-drive and PowerStore 5200Q 35-drive reference vectors with source citations"
    - "Zero .skip markers exist in any Dell-related test block"
    - "All 690+ existing tests continue to pass with no regressions"
  artifacts:
    - path: "tests/utils/units.spec.ts"
      provides: "Unit conversion utility test coverage"
      min_lines: 80
    - path: "tests/utils/connectivityConstraints.spec.ts"
      provides: "Connectivity constraint utility test coverage"
      min_lines: 40
    - path: "tests/fixtures/dell-vectors.ts"
      provides: "Dell Sizer reference test vectors"
      contains: "ME5224"
  key_links:
    - from: "tests/utils/units.spec.ts"
      to: "src/utils/units.ts"
      via: "import"
      pattern: "import.*from.*@utils/units"
    - from: "tests/utils/connectivityConstraints.spec.ts"
      to: "src/utils/connectivityConstraints.ts"
      via: "import"
      pattern: "import.*from.*@utils/connectivityConstraints"
---

<objective>
Raise test coverage from 70.5% to >=75% by testing the two highest-value uncovered utility modules (units.ts at 3.4% coverage = 56 uncovered statements, connectivityConstraints.ts at 16.7% = 10 uncovered statements). These 66 new statement covers push overall from 951/1349 to 1017/1349 = 75.4%.

Purpose: Satisfy Phase 13 success criterion 3 (75% coverage threshold) and confirm DELL-11/DELL-12 requirements are already met from prior phases.
Output: Two new test files, passing coverage threshold.
</objective>

<execution_context>
@/Users/fjacquet/.claude/get-shit-done/workflows/execute-plan.md
@/Users/fjacquet/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@vitest.config.ts
@src/utils/units.ts
@src/utils/connectivityConstraints.ts

<interfaces>
<!-- Key types and contracts the executor needs -->

From src/utils/units.ts:
```typescript
export const BINARY = { KiB: 1024, MiB: 1024**2, GiB: 1024**3, TiB: 1024**4, PiB: 1024**5 } as const
export const DECIMAL = { KB: 1000, MB: 1000**2, GB: 1000**3, TB: 1000**4, PB: 1000**5 } as const
export type UnitSystem = 'binary' | 'decimal'
export function formatBytes(bytes: number, system: UnitSystem = 'binary'): string
export function driveCapacityToBytes(decimalTB: number): number
export function bytesToDecimalTB(bytes: number): number
export function bytesToBinaryTiB(bytes: number): number
export function parseCapacity(str: string): number
export function getConversionFactor(): number
export function formatBytesBoth(bytes: number): { binary: string; decimal: string }
export function convertUnits(value: number, fromUnit: keyof typeof BINARY | keyof typeof DECIMAL, toUnit: keyof typeof BINARY | keyof typeof DECIMAL): number
```

From src/utils/connectivityConstraints.ts:
```typescript
export type ConnectivityConstraint = 'nvme_only' | 'flash_only' | 'none'
export interface ConnectivityConstraintInput { topologyType: TopologyType; nutanixOptions?: NutanixOptions; vsanOptions?: VsanOptions }
export interface ConnectivityConstraintResult { constraint: ConnectivityConstraint; validOptions: DriveConnectivity[]; reasonKey: string | null }
export function getConnectivityConstraint(input: ConnectivityConstraintInput): ConnectivityConstraintResult
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add comprehensive test coverage for src/utils/units.ts</name>
  <files>tests/utils/units.spec.ts</files>
  <read_first>
    - src/utils/units.ts
    - vitest.config.ts
    - tests/utils/validators.spec.ts (for test style reference)
  </read_first>
  <behavior>
    - formatBytes(0, 'binary') returns '0 B'
    - formatBytes(1024**4, 'binary') returns '1.0 TiB'
    - formatBytes(1024**5, 'binary') returns '1.0 PiB'
    - formatBytes(1024**3, 'binary') returns '1.0 GiB'
    - formatBytes(1024**2, 'binary') returns a MiB value
    - formatBytes(1024, 'binary') returns a KiB value
    - formatBytes(200 * 1024**4, 'binary') returns '200 TiB' (>=100 uses toFixed(0))
    - formatBytes(1000**4, 'decimal') returns '1.0 TB'
    - formatBytes(1000**5, 'decimal') returns '1.0 PB'
    - formatBytes(1000**3, 'decimal') returns '1.0 GB'
    - formatBytes(1000**2, 'decimal') returns a MB value
    - formatBytes(1000, 'decimal') returns a KB value
    - formatBytes(200 * 1000**4, 'decimal') returns '200 TB' (>=100 uses toFixed(0))
    - driveCapacityToBytes(1) returns 1_000_000_000_000
    - driveCapacityToBytes(3.84) returns 3_840_000_000_000
    - bytesToDecimalTB(1_000_000_000_000) returns 1
    - bytesToBinaryTiB(1024**4) returns 1
    - parseCapacity('24TB') returns 24_000_000_000_000
    - parseCapacity('24TiB') returns 24 * 1024**4
    - parseCapacity('1.92TB') returns 1_920_000_000_000
    - parseCapacity('100GB') returns 100_000_000_000
    - parseCapacity('100GiB') returns 100 * 1024**3
    - parseCapacity('512MiB') returns 512 * 1024**2
    - parseCapacity('512MB') returns 512_000_000
    - parseCapacity('1PiB') returns 1024**5
    - parseCapacity('1PB') returns 1000**5
    - parseCapacity('1KiB') returns 1024
    - parseCapacity('1KB') returns 1000
    - parseCapacity('1024B') returns 1024
    - parseCapacity('invalid') returns 0
    - parseCapacity('100') returns 100 * 1000**4 (defaults to TB)
    - getConversionFactor() returns approximately 0.9095
    - formatBytesBoth returns object with binary and decimal keys
    - convertUnits(1, 'TiB', 'GiB') returns 1024
    - convertUnits(1, 'TB', 'GB') returns 1000
    - convertUnits(1, 'TB', 'TiB') returns approximately 0.9095
  </behavior>
  <action>
    Create `tests/utils/units.spec.ts` with the following structure:

    1. Import all functions from `@utils/units`: formatBytes, driveCapacityToBytes, bytesToDecimalTB, bytesToBinaryTiB, parseCapacity, getConversionFactor, formatBytesBoth, convertUnits, BINARY, DECIMAL.

    2. Create a `describe('units.ts')` block with sub-describes:

    - `describe('BINARY and DECIMAL constants')`: verify BINARY.TiB === 1024**4, DECIMAL.TB === 1e12, etc.

    - `describe('formatBytes')`:
      - Test zero returns '0 B'
      - Test binary system: PiB, TiB, GiB, MiB, KiB thresholds
      - Test decimal system: PB, TB, GB, MB, KB thresholds
      - Test >=100 values use toFixed(0) (no decimal point): formatBytes(200 * BINARY.TiB, 'binary') should equal '200 TiB'
      - Test default system is 'binary'

    - `describe('driveCapacityToBytes')`: Test 1 TB, 3.84 TB, 24 TB conversions.

    - `describe('bytesToDecimalTB')`: Test round-trip with DECIMAL.TB.

    - `describe('bytesToBinaryTiB')`: Test round-trip with BINARY.TiB.

    - `describe('parseCapacity')`:
      - Test each unit: PiB, PB, TiB, TB, GiB, GB, MiB, MB, KiB, KB, B
      - Test decimal values ('1.92TB')
      - Test default unit (no unit specified defaults to TB)
      - Test invalid input returns 0
      - Test case insensitivity ('24tb' vs '24TB')

    - `describe('getConversionFactor')`: Test returns DECIMAL.TB / BINARY.TiB, approximately 0.9095.

    - `describe('formatBytesBoth')`: Test returns {binary, decimal} with correct formatted strings.

    - `describe('convertUnits')`:
      - Test binary-to-binary: TiB to GiB
      - Test decimal-to-decimal: TB to GB
      - Test cross-system: TB to TiB

    Use `toBeCloseTo()` for floating point comparisons. Use exact equality for integer-friendly conversions.
  </action>
  <verify>
    <automated>npx vitest run tests/utils/units.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - File `tests/utils/units.spec.ts` exists
    - All tests in `tests/utils/units.spec.ts` pass
    - grep for 'formatBytes' in `tests/utils/units.spec.ts` returns matches
    - grep for 'parseCapacity' in `tests/utils/units.spec.ts` returns matches
    - grep for 'convertUnits' in `tests/utils/units.spec.ts` returns matches
    - grep for 'getConversionFactor' in `tests/utils/units.spec.ts` returns matches
    - grep for 'formatBytesBoth' in `tests/utils/units.spec.ts` returns matches
    - At least 30 test cases (it blocks) in the file
  </acceptance_criteria>
  <done>All 8 exported functions from units.ts have test coverage; units.spec.ts passes with 30+ test cases covering all branches (binary/decimal, each unit threshold, edge cases)</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add comprehensive test coverage for src/utils/connectivityConstraints.ts</name>
  <files>tests/utils/connectivityConstraints.spec.ts</files>
  <read_first>
    - src/utils/connectivityConstraints.ts
    - src/types/topology.ts (for TopologyType, NutanixOptions, VsanOptions types)
    - src/types/drive.ts (for DriveConnectivity type)
  </read_first>
  <behavior>
    - getConnectivityConstraint({topologyType: 'powerstore'}) returns {constraint: 'nvme_only', validOptions: ['nvme'], reasonKey containing 'powerstore'}
    - getConnectivityConstraint({topologyType: 'vsan_esa'}) returns {constraint: 'nvme_only', validOptions: ['nvme'], reasonKey containing 'vsan_esa'}
    - getConnectivityConstraint({topologyType: 'powerflex'}) returns {constraint: 'flash_only', validOptions: ['all','nvme','sas','sata'], reasonKey containing 'powerflex'}
    - getConnectivityConstraint({topologyType: 'nutanix', nutanixOptions: {clusterType: 'all-flash', ...}}) returns {constraint: 'flash_only'}
    - getConnectivityConstraint({topologyType: 'vsan_osa', vsanOptions: {diskGroupMode: 'all-flash', ...}}) returns {constraint: 'flash_only'}
    - getConnectivityConstraint({topologyType: 'standard'}) returns {constraint: 'none', validOptions: ['all','nvme','sas','sata','hdd'], reasonKey: null}
    - getConnectivityConstraint({topologyType: 'zfs'}) returns {constraint: 'none'}
    - getConnectivityConstraint({topologyType: 'nutanix', nutanixOptions: {clusterType: 'hybrid', ...}}) returns {constraint: 'none'}
  </behavior>
  <action>
    Create `tests/utils/connectivityConstraints.spec.ts` with:

    1. Import `getConnectivityConstraint` from `@utils/connectivityConstraints`.
    2. Import required types: `ConnectivityConstraintInput` from the same module.

    3. Create `describe('connectivityConstraints')` block with sub-describes:

    - `describe('NVMe-only topologies')`:
      - Test powerstore returns nvme_only constraint with validOptions ['nvme']
      - Test vsan_esa returns nvme_only constraint with validOptions ['nvme']
      - Verify reasonKey contains the topology type string

    - `describe('Flash-only topologies')`:
      - Test powerflex returns flash_only with validOptions ['all', 'nvme', 'sas', 'sata'] (no 'hdd')
      - Test nutanix with clusterType 'all-flash' returns flash_only. Use a minimal NutanixOptions object: `{ clusterType: 'all-flash' } as any` (since only clusterType is checked)
      - Test vsan_osa with diskGroupMode 'all-flash' returns flash_only. Use: `{ diskGroupMode: 'all-flash' } as any`

    - `describe('No restriction')`:
      - Test standard topology returns none constraint with all 5 options including 'hdd'
      - Test zfs topology returns none
      - Test ceph topology returns none
      - Test nutanix with clusterType 'hybrid' returns none
      - Test vsan_osa without all-flash mode returns none
      - Verify reasonKey is null for no-restriction results

    Use exact equality checks for constraint type and validOptions arrays. Use `toContain` or `expect.arrayContaining` for validOptions verification.
  </action>
  <verify>
    <automated>npx vitest run tests/utils/connectivityConstraints.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - File `tests/utils/connectivityConstraints.spec.ts` exists
    - All tests pass
    - grep for 'nvme_only' in the test file returns matches
    - grep for 'flash_only' in the test file returns matches
    - grep for 'none' in the test file returns matches (constraint type)
    - At least 8 test cases covering all branches
  </acceptance_criteria>
  <done>All branches of getConnectivityConstraint are covered (nvme_only for powerstore/vsan_esa, flash_only for powerflex/nutanix-allflash/vsan_osa-allflash, none for everything else); test file passes</done>
</task>

<task type="auto">
  <name>Task 3: Verify coverage threshold passes and DELL-11/DELL-12 requirements are met</name>
  <files></files>
  <read_first>
    - tests/fixtures/dell-vectors.ts
    - vitest.config.ts
  </read_first>
  <action>
    Run the full test suite with coverage and verify all Phase 13 success criteria:

    1. Run `npx vitest run --coverage` and confirm the 75% threshold passes (exit code 0).

    2. Verify DELL-11: Confirm `tests/fixtures/dell-vectors.ts` exists with:
       - ME5224 ADAPT 12-drive reference vector (search for 'ME5224')
       - PowerStore 5200Q 35-drive reference vector (search for '5200Q')
       - TB-to-TiB normalization documented (search for 'TiB' in JSDoc comments)
       - Source citations on every vector (search for 'source:')

    3. Verify DELL-12: Confirm zero `.skip` markers in Dell test files:
       - Run `grep -rn '.skip' tests/engines/volumetry.spec.ts tests/engines/performance.spec.ts` and confirm 0 matches
       - Run `npx vitest run` and confirm all tests pass (690+ tests, 0 failures)

    4. Verify no regressions: All non-Dell topology tests still pass.

    If coverage threshold does NOT pass (unlikely but possible), identify which additional file needs a few more test cases and add them. The margin should be comfortable: units.ts alone adds ~56 statements, connectivityConstraints adds ~10, putting us at ~1017/1349 = 75.4%.
  </action>
  <verify>
    <automated>npx vitest run --coverage 2>&1; echo "EXIT: $?"</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run --coverage` exits with code 0 (thresholds pass)
    - grep for 'ME5224' in tests/fixtures/dell-vectors.ts returns at least 1 match
    - grep for '5200Q' in tests/fixtures/dell-vectors.ts returns at least 1 match
    - grep for '.skip' in tests/engines/volumetry.spec.ts returns 0 matches
    - grep for '.skip' in tests/engines/performance.spec.ts returns 0 matches
    - All 690+ tests pass with 0 failures
  </acceptance_criteria>
  <done>Coverage at or above 75% on all four metrics (statements, branches, functions, lines); DELL-11 vectors verified present with source citations; DELL-12 zero .skip markers confirmed; no test regressions</done>
</task>

</tasks>

<verification>
1. `npx vitest run --coverage` exits 0 (75% threshold on statements, branches, functions, lines)
2. `npx vitest run` shows 0 failures, 0 skipped Dell tests
3. `grep -c 'source:' tests/fixtures/dell-vectors.ts` returns 37 (all vectors have source citations)
4. `grep -rn '.skip' tests/engines/volumetry.spec.ts tests/engines/performance.spec.ts` returns 0 matches
</verification>

<success_criteria>
- Coverage threshold passes at 75%+ on all metrics
- All tests pass (690+, 0 failures)
- dell-vectors.ts has ME5224 and 5200Q reference vectors with source citations
- Zero .skip markers in Dell test files
- No regressions in non-Dell topology tests
</success_criteria>

<output>
After completion, create `.planning/phases/13-test-suite-cleanup/13-01-SUMMARY.md`
</output>
