---
phase: 07-dependency-maintenance
plan: 03
type: execute
wave: 2
depends_on:
  - "07-01"
  - "07-02"
files_modified:
  - src/test/setup.ts
  - biome.json
autonomous: false
requirements:
  - VERIFY-01
  - VERIFY-02
  - VERIFY-03
  - VERIFY-04

must_haves:
  truths:
    - "npm test completes with all tests green"
    - "npm run lint exits with zero errors"
    - "npm run typecheck exits with zero TypeScript errors"
    - "npm run build produces a successful production bundle"
    - "npm outdated shows no outdated entries for the five updated packages"
  artifacts:
    - path: "src/test/setup.ts"
      provides: "Test setup file compatible with jsdom 28.x"
    - path: "biome.json"
      provides: "Biome configuration compatible with 2.4.x rules"
  key_links:
    - from: "vitest.config.ts"
      to: "src/test/setup.ts"
      via: "setupFiles config"
      pattern: "setup\\.ts"
    - from: "biome.json"
      to: "src/**/*.ts"
      via: "biome check"
      pattern: "linter"
---

<objective>
Verify the updated dependencies work correctly and fix any issues introduced by the version bumps.

Purpose: The three verification gate commands (test, lint, typecheck, build) confirm the updates did not break existing functionality. Minor/major version bumps may require targeted fixes for new lint rules or changed type signatures.
Output: All four quality gate commands pass. Any compatibility issues from biome 2.4.x rules, jsdom 28.x API changes, or @types/node 25.x type changes are resolved.
</objective>

<execution_context>
@/Users/fjacquet/.claude/get-shit-done/workflows/execute-plan.md
@/Users/fjacquet/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/test/setup.ts
@biome.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run all quality gates and fix any compatibility issues</name>
  <files>src/test/setup.ts, biome.json</files>
  <action>
    Run each quality gate in sequence and fix any failures before proceeding to the next:

    **Step 1 — Tests:**
    ```
    npm run test:coverage -- --run
    ```
    If tests fail, investigate the error output:
    - jsdom 28.x may have removed deprecated APIs. Check if src/test/setup.ts uses any deprecated jsdom globals and update accordingly.
    - If `@testing-library/react` or `@testing-library/jest-dom` has peer warnings about jsdom, they are typically non-blocking. Only fix actual test failures.
    - Keep changes minimal — fix only what fails.

    **Step 2 — Lint:**
    ```
    npm run lint
    ```
    If lint fails with new biome 2.4.x rules:
    - Run `npm run lint:fix` to auto-fix safe violations.
    - For violations requiring manual decisions, use `// biome-ignore lint/...` suppression comment with a reason only as a last resort. Prefer fixing the code.
    - Do NOT run `biome migrate` — only fix lint rule violations in source files.
    - If biome.json itself needs updating for 2.4.x (e.g., renamed rule keys), update biome.json minimally.

    **Step 3 — TypeScript:**
    ```
    npm run typecheck
    ```
    If typecheck fails due to @types/node 25.x changes:
    - @types/node 25.x aligns with Node.js 25 API. Most changes are additive, but some deprecated types may be removed.
    - Check which files fail: likely src/vite-env.d.ts, vitest.config.ts, vite.config.ts, or utility files using Node.js built-ins.
    - Fix type errors minimally — prefer adding `/// <reference types="node" />` if missing, or using the updated type signatures.
    - Do NOT downgrade @types/node.

    **Step 4 — Build:**
    ```
    npm run build
    ```
    If build fails, it is likely a TypeScript error already caught in Step 3. Fix root cause there first.

    Apply all fixes in the same step — do not create separate commits per fix.
  </action>
  <verify>
    <automated>npm run test:coverage -- --run && npm run lint && npm run typecheck && npm run build && echo "ALL GATES PASSED"</automated>
  </verify>
  <done>
    - npm run test:coverage -- --run: exits 0, all tests green
    - npm run lint: exits 0, zero errors
    - npm run typecheck: exits 0, zero TypeScript errors
    - npm run build: exits 0, dist/ directory created
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    All five npm packages updated to target versions and all four quality gates pass:
    - dompurify 3.3.2 (production)
    - react-i18next 16.5.5 (production)
    - @biomejs/biome 2.4.6 (dev)
    - jsdom 28.1.0 (dev)
    - @types/node 25.3.3 (dev)
  </what-built>
  <how-to-verify>
    1. Run `npm outdated` — the five packages above should NOT appear in the output (they are now current)
    2. Run `npm run lint` — should exit 0 with no errors
    3. Run `npm run typecheck` — should exit 0 with no errors
    4. Run `npm run build` — should produce dist/ with no warnings
    5. Run `npm test -- --run` — all tests should be green
  </how-to-verify>
  <resume-signal>Type "approved" if all five checks pass, or describe which command failed and what error you see</resume-signal>
</task>

</tasks>

<verification>
Quality gate summary:
- `npm run test:coverage -- --run` exits 0
- `npm run lint` exits 0
- `npm run typecheck` exits 0
- `npm run build` exits 0
- `npm outdated` shows no output for the five updated packages
</verification>

<success_criteria>
All four quality gate commands exit with code 0. Human confirms npm outdated is clean for the five updated packages.
</success_criteria>

<output>
After completion, create `.planning/phases/07-dependency-maintenance/07-03-SUMMARY.md`
</output>
