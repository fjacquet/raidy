---
phase: 07-dependency-maintenance
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
autonomous: true
requirements:
  - DEVDEP-01
  - DEVDEP-02
  - DEVDEP-03

must_haves:
  truths:
    - "@biomejs/biome is at version 2.4.6 in node_modules"
    - "jsdom is at version 28.1.0 in package-lock.json"
    - "@types/node is at a version in the 25.x range in package-lock.json"
    - "npm install completes without errors"
  artifacts:
    - path: "package.json"
      provides: "Updated dev dependency version specifiers"
      contains: "@biomejs/biome"
    - path: "package-lock.json"
      provides: "Resolved dependency tree with updated dev dependencies"
  key_links:
    - from: "package.json"
      to: "node_modules/@biomejs/biome"
      via: "npm install"
      pattern: "\"version\": \"2\\.4\\."
---

<objective>
Update three dev npm dependencies to their latest versions.

Purpose: Keep linting toolchain (biome), test environment (jsdom), and Node type definitions (@types/node) current. Biome 2.4.x is a minor version bump — may introduce new lint rules. jsdom 28.x is a minor bump for the test environment. @types/node 25.x is a major bump — may have changed or removed some Node.js type signatures.
Output: Updated package.json and package-lock.json with all three dev dependencies at target versions.
</objective>

<execution_context>
@/Users/fjacquet/.claude/get-shit-done/workflows/execute-plan.md
@/Users/fjacquet/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update @biomejs/biome to 2.4.6</name>
  <files>package.json, package-lock.json</files>
  <action>
    Run the following command to update biome to the target minor version:

    ```
    npm install --save-dev @biomejs/biome@2.4.6
    ```

    Note: Biome uses an exact (unpinned) version in package.json — keep it exact (no ^ or ~). After updating, the biome binary in node_modules/.bin/biome will be 2.4.6. Do NOT run `biome migrate` or update biome.json config — only install the new version. Lint rule changes (if any) will be addressed in Plan 03 verification.
  </action>
  <verify>
    <automated>node -e "const p = JSON.parse(require('fs').readFileSync('package-lock.json','utf8')); const v = p.packages?.['node_modules/@biomejs/biome']?.version; if (!v?.startsWith('2.4.')) throw new Error('@biomejs/biome not at 2.4.x, got: ' + v); console.log('OK: @biomejs/biome', v)"</automated>
  </verify>
  <done>package-lock.json shows @biomejs/biome resolved to 2.4.6</done>
</task>

<task type="auto">
  <name>Task 2: Update jsdom and @types/node</name>
  <files>package.json, package-lock.json</files>
  <action>
    Run the following command to update both remaining dev dependencies together:

    ```
    npm install --save-dev jsdom@28.1.0 @types/node@25.3.3
    ```

    Key risks to watch for:
    - jsdom 28.x: May remove deprecated APIs used in test setup (src/test/setup.ts). If npm install shows peer warnings from vitest or @testing-library/react, accept them — they are typically non-blocking for jsdom minor bumps.
    - @types/node 25.x: Major version bump. Type signatures for Node.js built-ins (Buffer, process, fs, etc.) may have changed. Actual type errors will surface during `npm run typecheck` in Plan 03 — do not attempt to fix them here.

    Do NOT run npm audit fix or update any other packages. Accept the exact versions 28.1.0 and 25.3.3.
  </action>
  <verify>
    <automated>node -e "const p = JSON.parse(require('fs').readFileSync('package-lock.json','utf8')); const jsdom = p.packages?.['node_modules/jsdom']?.version; const types = p.packages?.['node_modules/@types/node']?.version; if (!jsdom?.startsWith('28.')) throw new Error('jsdom not at 28.x, got: ' + jsdom); if (!types?.startsWith('25.')) throw new Error('@types/node not at 25.x, got: ' + types); console.log('OK: jsdom', jsdom, '| @types/node', types)"</automated>
  </verify>
  <done>package-lock.json shows jsdom at 28.1.0 and @types/node at 25.3.3</done>
</task>

</tasks>

<verification>
All three dev dependencies updated:
- `npm list @biomejs/biome` shows 2.4.6
- `npm list jsdom` shows 28.1.0
- `npm list @types/node` shows 25.3.3
- `npm install` exits 0
</verification>

<success_criteria>
- @biomejs/biome at 2.4.6 in package-lock.json
- jsdom at 28.1.0 in package-lock.json
- @types/node at 25.3.3 in package-lock.json
- No npm install errors
</success_criteria>

<output>
After completion, create `.planning/phases/07-dependency-maintenance/07-02-SUMMARY.md`
</output>
