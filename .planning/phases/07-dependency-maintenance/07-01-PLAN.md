---
phase: 07-dependency-maintenance
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
autonomous: true
requirements:
  - DEP-01
  - DEP-02

must_haves:
  truths:
    - "dompurify is at version 3.3.2 in package-lock.json"
    - "react-i18next is at version 16.5.5 in package-lock.json"
    - "npm install completes without errors"
  artifacts:
    - path: "package.json"
      provides: "Updated production dependency versions"
      contains: "dompurify"
    - path: "package-lock.json"
      provides: "Resolved dependency tree with updated versions"
  key_links:
    - from: "package.json"
      to: "package-lock.json"
      via: "npm install"
      pattern: "\"dompurify\": \"3\\.3\\.2\""
---

<objective>
Update two production npm dependencies to their latest patch versions.

Purpose: Keep security-sensitive (dompurify) and i18n (react-i18next) libraries current. Both are patch releases — no breaking changes expected.
Output: Updated package.json and package-lock.json with dompurify 3.3.2 and react-i18next 16.5.5.
</objective>

<execution_context>
@/Users/fjacquet/.claude/get-shit-done/workflows/execute-plan.md
@/Users/fjacquet/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update dompurify to 3.3.2</name>
  <files>package.json, package-lock.json</files>
  <action>
    Run the following command to update dompurify to its latest patch version:

    ```
    npm install dompurify@3.3.2
    ```

    This updates both package.json (bumps the version specifier) and package-lock.json (resolves to exact 3.3.2).

    Do NOT run npm audit fix or update any other packages — scope is strictly dompurify only for this task.
  </action>
  <verify>
    <automated>node -e "const p = JSON.parse(require('fs').readFileSync('package-lock.json','utf8')); const v = p.packages?.['node_modules/dompurify']?.version || p.dependencies?.dompurify?.version; if (!v?.startsWith('3.3.2')) throw new Error('dompurify not at 3.3.2, got: ' + v); console.log('OK: dompurify', v)"</automated>
  </verify>
  <done>package-lock.json shows dompurify resolved to 3.3.2</done>
</task>

<task type="auto">
  <name>Task 2: Update react-i18next to 16.5.5</name>
  <files>package.json, package-lock.json</files>
  <action>
    Run the following command to update react-i18next to its latest patch version:

    ```
    npm install react-i18next@16.5.5
    ```

    This updates both package.json and package-lock.json. Do NOT update i18next or any peer dependencies unless npm requires it for compatibility (in which case, accept the minimum required version bump only).
  </action>
  <verify>
    <automated>node -e "const p = JSON.parse(require('fs').readFileSync('package-lock.json','utf8')); const v = p.packages?.['node_modules/react-i18next']?.version || p.dependencies?.['react-i18next']?.version; if (!v?.startsWith('16.5.5')) throw new Error('react-i18next not at 16.5.5, got: ' + v); console.log('OK: react-i18next', v)"</automated>
  </verify>
  <done>package-lock.json shows react-i18next resolved to 16.5.5</done>
</task>

</tasks>

<verification>
Both production dependencies updated:
- `npm list dompurify` shows 3.3.2
- `npm list react-i18next` shows 16.5.5
- `npm install` exits 0 with no peer conflict errors
</verification>

<success_criteria>
- dompurify at 3.3.2 in package-lock.json
- react-i18next at 16.5.5 in package-lock.json
- No npm peer dependency errors
</success_criteria>

<output>
After completion, create `.planning/phases/07-dependency-maintenance/07-01-SUMMARY.md`
</output>
