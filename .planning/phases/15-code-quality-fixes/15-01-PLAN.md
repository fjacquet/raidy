---
phase: 15-code-quality-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/workers/resilienceWorker.ts
autonomous: true
requirements:
  - QUALITY-01
must_haves:
  truths:
    - "npm run typecheck reports zero noNonNullAssertion warnings originating from resilienceWorker.ts"
    - "All Monte Carlo simulation logic produces identical results before and after the fix (no behavioral change)"
    - "npm run lint reports no new errors in resilienceWorker.ts"
  artifacts:
    - path: "src/workers/resilienceWorker.ts"
      provides: "Non-null-assertion-free Monte Carlo simulation worker"
      contains: "no ! on array index access"
  key_links:
    - from: "src/workers/resilienceWorker.ts"
      to: "mirrorGroupFailures / groupFailures arrays"
      via: "nullish coalescing ?? 0 instead of non-null assertion !"
      pattern: "\\?\\? 0"
---

# Plan 15-01: Fix resilienceWorker.ts noNonNullAssertion warnings (QUALITY-01)

<objective>
Eliminate all Biome noNonNullAssertion warnings in src/workers/resilienceWorker.ts by replacing
array index non-null assertions (!) with nullish coalescing (?? 0) equivalents.

Purpose: The arrays (mirrorGroupFailures, groupFailures, survivingPerGroup) are all initialized
with .fill(0) and typed as number[], so TypeScript sees index access as number | undefined even
though the values are always present. The fix makes the code statically safe without changing
any runtime behavior — (arr[i] ?? 0) returns the same numeric value in every case arr[i]!
would have.

Output: A clean resilienceWorker.ts with zero ! on array indices and identical simulation logic.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/workers/resilienceWorker.ts

<interfaces>
<!-- Arrays involved — all are number[] initialized via new Array(N).fill(0) -->
<!-- TypeScript infers index access as number | undefined, triggering noNonNullAssertion -->

mirrorGroupFailures: number[]   // length = numMirrorGroups, each element always 0+
groupFailures: number[]         // length = numGroups, each element always 0+
survivingPerGroup: number[]     // derived via .map(), each element always 0+

-- Pattern inventory (all occurrences of ! on array index in the file) --

Line 198:  effectiveMirrorCopies - mirrorGroupFailures[g]!
           → effectiveMirrorCopies - (mirrorGroupFailures[g] ?? 0)

Line 205:  r -= survivingPerGroup[g]!
           → r -= (survivingPerGroup[g] ?? 0)

Line 212:  mirrorGroupFailures[hitGroup]!++
           → mirrorGroupFailures[hitGroup] = (mirrorGroupFailures[hitGroup] ?? 0) + 1

Line 215:  mirrorGroupFailures[hitGroup]! > mirrorParityPerGroup
           → (mirrorGroupFailures[hitGroup] ?? 0) > mirrorParityPerGroup

Line 227:  mirrorGroupFailures[hitGroup]! >= mirrorParityPerGroup
           → (mirrorGroupFailures[hitGroup] ?? 0) >= mirrorParityPerGroup

Line 234:  drivesPerGroup - groupFailures[g]!
           → drivesPerGroup - (groupFailures[g] ?? 0)

Line 241:  r -= survivingPerGroup[g]!
           → r -= (survivingPerGroup[g] ?? 0)

Line 248:  groupFailures[hitGroup]!++
           → groupFailures[hitGroup] = (groupFailures[hitGroup] ?? 0) + 1

Line 251:  groupFailures[hitGroup]! > parityPerGroup
           → (groupFailures[hitGroup] ?? 0) > parityPerGroup

Line 262:  groupFailures[hitGroup]! >= parityPerGroup  (first occurrence)
           → (groupFailures[hitGroup] ?? 0) >= parityPerGroup

Line 267:  groupFailures[hitGroup]! >= parityPerGroup  (second occurrence)
           → (groupFailures[hitGroup] ?? 0) >= parityPerGroup

Line 307:  mirrorGroupFailures[g]! > mirrorGroupFailures[maxIdx]!
           → (mirrorGroupFailures[g] ?? 0) > (mirrorGroupFailures[maxIdx] ?? 0)

Line 309:  if (mirrorGroupFailures[maxIdx]! > 0) mirrorGroupFailures[maxIdx]!--
           → const cur = mirrorGroupFailures[maxIdx] ?? 0
             if (cur > 0) mirrorGroupFailures[maxIdx] = cur - 1

Line 315:  groupFailures[g]! > groupFailures[maxIdx]!
           → (groupFailures[g] ?? 0) > (groupFailures[maxIdx] ?? 0)

Line 317:  if (groupFailures[maxIdx]! > 0) groupFailures[maxIdx]!--
           → const cur = groupFailures[maxIdx] ?? 0
             if (cur > 0) groupFailures[maxIdx] = cur - 1
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace all non-null assertions on array indices with nullish coalescing</name>
  <files>src/workers/resilienceWorker.ts</files>
  <action>
Apply the following targeted replacements in src/workers/resilienceWorker.ts. Make each
replacement exactly — the surrounding code must not change. This is a pure mechanical
substitution with no logic changes.

The complete set of changes (use the line numbers in the context <interfaces> block as a
guide — verify visually before writing):

1. Line ~198 (inside mirrorGroupFailures.map callback):
   OLD: effectiveMirrorCopies - mirrorGroupFailures[g]!
   NEW: effectiveMirrorCopies - (mirrorGroupFailures[g] ?? 0)

2. Line ~205 (loop subtracting from r for mirror group selection):
   OLD: r -= survivingPerGroup[g]!
   NEW: r -= (survivingPerGroup[g] ?? 0)

3. Line ~212 (increment mirrorGroupFailures):
   OLD: mirrorGroupFailures[hitGroup]!++
   NEW: mirrorGroupFailures[hitGroup] = (mirrorGroupFailures[hitGroup] ?? 0) + 1

4. Line ~215 (data loss check mirror):
   OLD: mirrorGroupFailures[hitGroup]! > mirrorParityPerGroup
   NEW: (mirrorGroupFailures[hitGroup] ?? 0) > mirrorParityPerGroup

5. Line ~227 (URE check mirror):
   OLD: mirrorGroupFailures[hitGroup]! >= mirrorParityPerGroup
   NEW: (mirrorGroupFailures[hitGroup] ?? 0) >= mirrorParityPerGroup

6. Line ~234 (inside groupFailures.map callback):
   OLD: drivesPerGroup - groupFailures[g]!
   NEW: drivesPerGroup - (groupFailures[g] ?? 0)

7. Line ~241 (loop subtracting from r for group selection):
   OLD: r -= survivingPerGroup[g]!
   NEW: r -= (survivingPerGroup[g] ?? 0)

8. Line ~248 (increment groupFailures):
   OLD: groupFailures[hitGroup]!++
   NEW: groupFailures[hitGroup] = (groupFailures[hitGroup] ?? 0) + 1

9. Line ~251 (data loss check group):
   OLD: groupFailures[hitGroup]! > parityPerGroup
   NEW: (groupFailures[hitGroup] ?? 0) > parityPerGroup

10. Lines ~262 and ~267 (URE check group — two identical expressions):
    OLD: groupFailures[hitGroup]! >= parityPerGroup
    NEW: (groupFailures[hitGroup] ?? 0) >= parityPerGroup
    (Replace both occurrences)

11. Line ~307 (mirror rebuild: find most degraded group comparison):
    OLD: mirrorGroupFailures[g]! > mirrorGroupFailures[maxIdx]!
    NEW: (mirrorGroupFailures[g] ?? 0) > (mirrorGroupFailures[maxIdx] ?? 0)

12. Line ~309 (mirror rebuild: decrement most degraded group):
    OLD: if (mirrorGroupFailures[maxIdx]! > 0) mirrorGroupFailures[maxIdx]!--
    NEW: { const cur = mirrorGroupFailures[maxIdx] ?? 0; if (cur > 0) mirrorGroupFailures[maxIdx] = cur - 1 }

13. Line ~315 (group rebuild: find most degraded group comparison):
    OLD: groupFailures[g]! > groupFailures[maxIdx]!
    NEW: (groupFailures[g] ?? 0) > (groupFailures[maxIdx] ?? 0)

14. Line ~317 (group rebuild: decrement most degraded group):
    OLD: if (groupFailures[maxIdx]! > 0) groupFailures[maxIdx]!--
    NEW: { const cur = groupFailures[maxIdx] ?? 0; if (cur > 0) groupFailures[maxIdx] = cur - 1 }

After all replacements, verify with grep that zero occurrences of `!` remain on array index
access patterns (i.e., no `[...]!` pattern remains in the file).
  </action>
  <verify>
    <automated>cd /Users/fjacquet/Projects/raidy && npm run typecheck 2>&1 | grep -E "noNonNullAssertion|resilienceWorker" | wc -l</automated>
  </verify>
  <done>
    - `npm run typecheck` output contains zero lines mentioning noNonNullAssertion or resilienceWorker
    - `grep -n '\[.*\]!' src/workers/resilienceWorker.ts` returns no matches
    - `npm run lint` reports no new errors for resilienceWorker.ts
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify no behavioral regression in simulation logic</name>
  <files>src/workers/resilienceWorker.ts</files>
  <action>
Run the full test suite and typecheck to confirm the fix introduced no regressions.

Steps:
1. Run `npm run typecheck` — must pass with zero errors
2. Run `npm test -- --reporter=verbose 2>&1 | tail -20` — must pass
3. Run `npm run lint 2>&1 | grep resilienceWorker` — must return empty (no new lint issues)

No code changes in this task — verification only.
  </action>
  <verify>
    <automated>cd /Users/fjacquet/Projects/raidy && npm run typecheck && npm test -- --run 2>&1 | tail -5</automated>
  </verify>
  <done>
    - `npm run typecheck` exits 0
    - `npm test --run` exits 0 (all tests pass)
    - `npm run lint` exits 0 (no lint errors)
  </done>
</task>

</tasks>

<verification>
Final checks to confirm QUALITY-01 is resolved:

1. `grep -n '\[.*\]!' src/workers/resilienceWorker.ts` → zero matches
2. `npm run typecheck 2>&1 | grep noNonNullAssertion` → zero output lines
3. `npm test --run` → exits 0, no test failures
4. `npm run lint` → exits 0, no new errors
</verification>

<success_criteria>
- Zero noNonNullAssertion warnings from resilienceWorker.ts in typecheck output
- All existing tests pass (npm test --run exits 0)
- No new lint errors introduced
- Runtime behavior of Monte Carlo simulation unchanged (same algorithm, same array accesses)
</success_criteria>

<output>
After completion, create `.planning/phases/15-code-quality-fixes/15-code-quality-fixes-01-SUMMARY.md`
</output>
