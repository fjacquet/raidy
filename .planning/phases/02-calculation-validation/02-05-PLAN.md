---
phase: 02-calculation-validation
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/utils/urlStorage.spec.ts
  - tests/utils/validators.spec.ts
autonomous: true

must_haves:
  truths:
    - "URL state serialization roundtrip preserves all configuration values accurately"
    - "Backward compatibility maintained for legacy URL formats (v1.0 -> v2.0 migration)"
    - "Form validators prevent invalid configurations (min drives, incompatible topologies)"
    - "Validation errors provide clear messages for user-facing error handling"
  artifacts:
    - path: "tests/utils/urlStorage.spec.ts"
      provides: "URL serialization/deserialization tests with backward compatibility"
      contains: "roundtrip|legacy.*format|backward.*compatibility"
      min_tests: 12
    - path: "tests/utils/validators.spec.ts"
      provides: "Form validation rule tests for all configuration constraints"
      contains: "validateDriveCount|validateTopology|min.*drives"
      min_tests: 15
  key_links:
    - from: "Roundtrip tests"
      to: "serializeToURL + deserializeFromURL"
      via: "config -> serialize -> deserialize -> should equal config"
      pattern: "serialize.*deserialize|roundtrip"
    - from: "Legacy migration tests"
      to: "Version detection and format conversion"
      via: "v1.0 format -> v2.0 format"
      pattern: "v=1\\.0|migrate|legacy"
---

<objective>
Validate URL state serialization for shareable links and form validation rules for configuration constraints.

Purpose: URL state serialization enables "share link" functionality - if roundtrip fails, shared links break. Form validation prevents users from creating impossible configurations (e.g., RAID 5 with 2 drives).

Output: Comprehensive tests for URL storage and validation utilities ensuring configuration state is preserved and validated correctly.
</objective>

<execution_context>
@/Users/fjacquet/Projects/raidy/.claude/get-shit-done/workflows/execute-plan.md
@/Users/fjacquet/Projects/raidy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/fjacquet/Projects/raidy/.planning/PROJECT.md
@/Users/fjacquet/Projects/raidy/.planning/ROADMAP.md
@/Users/fjacquet/Projects/raidy/.planning/REQUIREMENTS.md
@/Users/fjacquet/Projects/raidy/.planning/phases/02-calculation-validation/02-RESEARCH.md

# Files to test
@/Users/fjacquet/Projects/raidy/src/store/urlStorage.ts
@/Users/fjacquet/Projects/raidy/src/utils/validators.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create URL serialization roundtrip tests with snapshot validation</name>
  <files>tests/utils/urlStorage.spec.ts</files>
  <action>
    Create tests/utils/urlStorage.spec.ts for URL state management tests.

    Add describe block: "URL Storage - Serialization Roundtrip"

    Roundtrip test pattern (TEST-14):
    - Create configuration object with all fields populated
    - Serialize to URL string: serialized = serializeToURL(config)
    - Deserialize back: deserialized = deserializeFromURL(serialized)
    - Assert: expect(deserialized).toEqual(config)
    - All values must match exactly after roundtrip

    Test configurations to validate:
    - Standard RAID configuration:
      - topology: { type: 'standard', level: 'RAID5' }
      - driveCount: 8
      - driveModel: 'wd-gold-12tb'
      - hotSpares: 1
    - ZFS configuration:
      - topology: { type: 'zfs', level: 'raidz2' }
      - zfsOptions: { compression: 'lz4', ashift: 12 }
    - vSAN ESA configuration:
      - topology: { type: 'vsan', vsanType: 'esa', ftt: 1 }
      - serverCount: 8 (affects adaptive efficiency)
    - Complete configuration (all fields):
      - Hardware, topology, workload, advanced settings
      - Ensure no fields lost in serialization

    Add snapshot tests:
    - Snapshot current URL format: expect(serialized).toMatchSnapshot()
    - If format changes unintentionally, snapshot will catch it
    - Prevents breaking existing shared links

    Test edge cases:
    - Empty/minimal configuration
    - Maximum complexity configuration
    - Special characters in custom fields
    - Very long configurations (compression effectiveness)
  </action>
  <verify>
    ls -la tests/utils/urlStorage.spec.ts (file exists)
    npm test -- urlStorage (all roundtrip tests pass)
    grep -c "roundtrip\\|serialize.*deserialize" tests/utils/urlStorage.spec.ts (at least 5)
  </verify>
  <done>
    - urlStorage.spec.ts created
    - Roundtrip tests validate config preservation
    - Snapshot tests prevent format regression
    - Edge cases tested (empty, complex, special chars)
    - All roundtrip tests passing
  </done>
</task>

<task type="auto">
  <name>Task 2: Add backward compatibility migration tests</name>
  <files>tests/utils/urlStorage.spec.ts</files>
  <action>
    Expand urlStorage.spec.ts with backward compatibility tests.

    Add describe block: "URL Storage - Backward Compatibility"

    Legacy format migration (TEST-14 backward compatibility):
    - Identify historical URL formats (if any exist)
    - Test migration from old format to current format
    - Pattern: oldFormat -> deserialize -> should convert to new structure

    Example legacy format tests:
    - v1.0 format (if exists): ?v=1.0&t=5&d=8&m=wd-gold-12tb
      - Deserialize should detect v=1.0
      - Migrate to v2.0 structure
      - topology.level should map from numeric code (t=5 -> RAID5)
    - Missing version field: assume oldest format, migrate
    - Partial configuration: fill missing fields with defaults

    Validate migration correctness:
    - Legacy RAID 5 config -> deserializes to { type: 'standard', level: 'RAID5' }
    - Drive model preserved
    - Drive count preserved
    - Missing fields filled with sensible defaults

    Re-serialization test:
    - Legacy format -> deserialize -> re-serialize
    - Should output new format (v2.0 or current)
    - Should NOT output legacy format again
    - Validates migration is one-way upgrade

    Snapshot legacy formats:
    - Keep test fixtures for all historical formats
    - Ensures ongoing support even as code evolves
    - Documents format evolution

    If no legacy formats exist yet:
    - Create placeholder tests for future versioning
    - Document expected versioning strategy
    - Prepare for v2.0 when breaking changes needed
  </action>
  <verify>
    npm test -- urlStorage (backward compatibility tests pass)
    grep -c "legacy\\|migrate\\|v1\\.0\\|backward.*compatibility" tests/utils/urlStorage.spec.ts (at least 3)
    grep "version" tests/utils/urlStorage.spec.ts (version detection tested)
  </verify>
  <done>
    - Backward compatibility tests added
    - Legacy format migration validated (or documented if none exist yet)
    - Re-serialization to new format tested
    - Version detection logic tested
    - Shared links from previous versions will continue working
  </done>
</task>

<task type="auto">
  <name>Task 3: Create form validator tests for all validation rules</name>
  <files>tests/utils/validators.spec.ts</files>
  <action>
    Create tests/utils/validators.spec.ts for configuration validation tests.

    Add describe block: "Validators - Drive Count Validation"

    Drive count validation (TEST-15):
    - Test minimum drive requirements per topology:
      - RAID 0: min 2 drives
      - RAID 1: min 2 drives (even number)
      - RAID 5: min 3 drives
      - RAID 6: min 4 drives
      - RAID 10: min 4 drives (even number)
      - ZFS Mirror: min 2 drives
      - ZFS RAID-Z1: min 3 drives
      - ZFS RAID-Z2: min 4 drives
      - ZFS RAID-Z3: min 5 drives
      - vSAN: min drives based on FTT setting
    - Test invalid configurations return error
    - Test valid configurations return null (no error)
    - Validate error messages are user-friendly

    Add describe block: "Validators - Topology Compatibility"

    Topology compatibility validation:
    - Test incompatible combinations:
      - vSAN ESA requires NVMe drives (not HDD/SATA)
      - S2D requires minimum node count for parity
      - ZFS dRAID requires specific drive count formulas
    - Test drive type restrictions:
      - Some topologies require specific drive types
      - Validate error when wrong type selected

    Add describe block: "Validators - Configuration Constraints"

    General configuration validation:
    - Hot spares cannot exceed total drives
    - Hot spares make sense for redundant RAID (not RAID 0)
    - Server count requirements (vSAN, S2D, Ceph)
    - Chunk size must be power of 2 (RAID)
    - Record size constraints (ZFS)

    Property-based tests:
    - Adding drives never makes valid config invalid (monotonicity)
    - Validators are pure functions (same input -> same output)

    Test error message quality:
    - Messages should explain WHY config is invalid
    - Messages should suggest how to fix
    - Example: "RAID 5 requires at least 3 drives. You have 2 drives. Add 1 more drive."
    - Not just: "Invalid configuration"

    Use table-driven tests:
    - Array of { config, expectedError, description }
    - describe.each pattern for systematic coverage
  </action>
  <verify>
    ls -la tests/utils/validators.spec.ts (file exists)
    npm test -- validators (all validation tests pass)
    grep -c "validateDriveCount\\|validateTopology\\|min.*drives" tests/utils/validators.spec.ts (at least 10)
    grep "error.*message\\|user-friendly" tests/utils/validators.spec.ts (message quality tested)
  </verify>
  <done>
    - validators.spec.ts created
    - Drive count validation tested for all topologies
    - Topology compatibility validation tested
    - Configuration constraint validation tested
    - Error messages validated for user-friendliness
    - Property-based tests validate validator behavior
    - TEST-15 requirement satisfied
  </done>
</task>

</tasks>

<verification>
Run full test suite with coverage:
```bash
npm run test:coverage
```

Expected results:
- urlStorage tests: 15+ tests passing
- validators tests: 20+ tests passing
- URL roundtrip preserves all configuration values
- Backward compatibility maintained for legacy formats
- Form validation prevents invalid configurations
- Utils coverage approaching 75% threshold
- Overall project coverage at or above 75% (Phase 2 complete)
- TEST-14 and TEST-15 requirements satisfied
</verification>

<success_criteria>
- [ ] urlStorage.spec.ts created with roundtrip tests
- [ ] Snapshot tests prevent URL format regression
- [ ] Backward compatibility tests validate legacy format migration
- [ ] validators.spec.ts created with drive count validation
- [ ] Topology compatibility validation tested
- [ ] Configuration constraint validation tested
- [ ] Error messages validated for clarity and helpfulness
- [ ] All new tests passing
- [ ] Utils coverage increases toward 75% threshold
- [ ] Overall project coverage reaches 75% threshold (Phase 2 complete)
</success_criteria>

<output>
After completion, create `.planning/phases/02-calculation-validation/02-05-SUMMARY.md` following summary template.
</output>
