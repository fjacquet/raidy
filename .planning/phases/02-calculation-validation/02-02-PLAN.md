---
phase: 02-calculation-validation
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - tests/engines/volumetry.spec.ts
  - tests/fixtures/zfs-vectors.ts
  - tests/fixtures/vsan-vectors.ts
autonomous: true

must_haves:
  truths:
    - "ZFS slop factor calculations match OpenZFS documentation (1/32 with min/max bounds)"
    - "vSAN ESA adaptive efficiency matches VMware specs (67-80% based on cluster size)"
    - "S2D, Ceph, and Nutanix capacity calculations validated against vendor documentation"
  artifacts:
    - path: "tests/fixtures/zfs-vectors.ts"
      provides: "OpenZFS-validated test vectors for ZFS topologies"
      min_lines: 50
    - path: "tests/fixtures/vsan-vectors.ts"
      provides: "VMware-validated test vectors for vSAN ESA/OSA efficiency"
      min_lines: 30
    - path: "tests/engines/volumetry.spec.ts"
      provides: "ZFS and advanced storage topology tests"
      contains: "ZFS slop factor|vSAN.*adaptive|S2D|Ceph|Nutanix"
      min_tests: 30
  key_links:
    - from: "ZFS tests"
      to: "OpenZFS slop factor formula"
      via: "1/32 calculation with 128MiB min, 128GiB max"
      pattern: "slopOverhead.*1/32|128.*MiB|128.*GiB"
    - from: "vSAN tests"
      to: "VMware adaptive efficiency formula"
      via: "cluster size -> stripe width mapping"
      pattern: "serverCount.*efficiency"
---

<objective>
Validate advanced storage topology calculations (ZFS, vSAN ESA/OSA, S2D, Ceph, Nutanix) against vendor documentation and industry specifications.

Purpose: Modern storage systems use complex efficiency models (ZFS slop factor, vSAN adaptive RAID, S2D rebuild reserve). Accurate modeling of these systems is critical for enterprise storage planning.

Output: Comprehensive test coverage for all advanced storage topologies with vendor-validated test vectors.
</objective>

<execution_context>
@/Users/fjacquet/Projects/raidy/.claude/get-shit-done/workflows/execute-plan.md
@/Users/fjacquet/Projects/raidy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/fjacquet/Projects/raidy/.planning/PROJECT.md
@/Users/fjacquet/Projects/raidy/.planning/ROADMAP.md
@/Users/fjacquet/Projects/raidy/.planning/REQUIREMENTS.md
@/Users/fjacquet/Projects/raidy/.planning/phases/02-calculation-validation/02-RESEARCH.md

# Prior plan output
@/Users/fjacquet/Projects/raidy/.planning/phases/02-calculation-validation/02-01-SUMMARY.md

# Current test file to expand
@/Users/fjacquet/Projects/raidy/tests/engines/volumetry.spec.ts
@/Users/fjacquet/Projects/raidy/src/engines/volumetry/index.ts
@/Users/fjacquet/Projects/raidy/src/types/topology.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ZFS test vectors and expand ZFS topology tests</name>
  <files>
    tests/fixtures/zfs-vectors.ts
    tests/engines/volumetry.spec.ts
  </files>
  <action>
    Create tests/fixtures/zfs-vectors.ts with OpenZFS-validated test vectors:
    - Export const zfsVectors array with test cases for:
      - ZFS Stripe (simple concatenation)
      - ZFS Mirror (2-way, 3-way)
      - ZFS RAID-Z1/Z2/Z3 (various drive counts)
      - ZFS dRAID1/dRAID2/dRAID3 (distributed RAID)
    - Each vector includes:
      - name, level, drives, driveSize
      - expectedUsable (accounting for slop factor)
      - slopOverhead (1/32 of pool with 128MiB min, 128GiB max)
      - tolerance (2-3% for slop + filesystem overhead)
      - source: "OpenZFS Documentation"
      - url: Link to OpenZFS specs

    Expand volumetry.spec.ts with ZFS tests:
    - Add new describe block: "Volumetry Engine - ZFS Topologies"
    - Table-driven tests using describe.each(zfsVectors)
    - Validate usableCapacity within tolerance
    - Add specific tests for slop factor edge cases:
      - Small pool (< 4TB): slop >= 128MiB minimum
      - Large pool (> 4PB): slop <= 128GiB maximum
      - Standard pool: slop = rawCapacity / 32
    - Property-based test: ZFS efficiency always < 100% (slop overhead always present)

    Reference OpenZFS documentation formulas:
    - spa_slop_shift = 5 (default, means 1/32)
    - Minimum slop: 128MiB
    - Maximum slop: 128GiB
    - Formula: clamp(rawCapacity / 32, 128MiB, 128GiB)
  </action>
  <verify>
    ls -la tests/fixtures/zfs-vectors.ts (file exists)
    grep -c "zfs.*stripe\\|mirror\\|raidz" tests/fixtures/zfs-vectors.ts (at least 8 vectors)
    npm test -- volumetry (ZFS tests pass)
    grep "slopOverhead" tests/engines/volumetry.spec.ts (slop factor validation present)
  </verify>
  <done>
    - zfs-vectors.ts created with OpenZFS-validated test data
    - ZFS topology tests added to volumetry.spec.ts
    - Slop factor edge cases tested (min/max bounds)
    - Property-based test validates slop overhead always present
    - Tests pass and validate against OpenZFS specifications
  </done>
</task>

<task type="auto">
  <name>Task 2: Add vSAN ESA/OSA adaptive efficiency tests</name>
  <files>
    tests/fixtures/vsan-vectors.ts
    tests/engines/volumetry.spec.ts
  </files>
  <action>
    Create tests/fixtures/vsan-vectors.ts with VMware-validated test vectors:
    - vSAN OSA (Original Storage Architecture):
      - RAID-1 (mirroring): 50% efficiency
      - RAID-5: 75% efficiency (3+1)
      - RAID-6: 66.67% efficiency (4+2)
      - Varies by FTT (Failures To Tolerate) setting
    - vSAN ESA (Express Storage Architecture):
      - Adaptive RAID-5: 67-80% efficiency based on cluster size
      - Small clusters (3-5 hosts): 2+1 scheme = 66.67% efficiency
      - Large clusters (6+ hosts): 4+1 scheme = 80% efficiency
    - Include serverCount in test vectors for ESA adaptive logic

    Expand volumetry.spec.ts with vSAN tests:
    - Add describe block: "Volumetry Engine - vSAN Topologies"
    - OSA tests: validate fixed efficiency by RAID level
    - ESA tests: validate adaptive efficiency based on serverCount
    - Test ESA threshold: serverCount < 6 uses 2+1, >= 6 uses 4+1
    - Property-based test: ESA efficiency increases with cluster size

    Reference VMware documentation:
    - vSAN 8 blog post on adaptive RAID-5
    - FTT=1 RAID-5: 75% efficiency (OSA)
    - FTT=1 Erasure Coding: 67-80% efficiency (ESA, adaptive)
  </action>
  <verify>
    ls -la tests/fixtures/vsan-vectors.ts (file exists)
    grep -c "serverCount" tests/fixtures/vsan-vectors.ts (ESA vectors include cluster size)
    npm test -- volumetry (vSAN tests pass)
    grep "adaptive.*efficiency\\|ESA" tests/engines/volumetry.spec.ts (ESA logic tested)
  </verify>
  <done>
    - vsan-vectors.ts created with VMware-validated efficiency data
    - vSAN OSA and ESA tests added to volumetry.spec.ts
    - Adaptive efficiency logic validated (cluster size -> stripe width)
    - Tests confirm ESA efficiency ranges match VMware specs (67-80%)
    - All vSAN tests passing
  </done>
</task>

<task type="auto">
  <name>Task 3: Add S2D, Ceph, and Nutanix topology tests</name>
  <files>tests/engines/volumetry.spec.ts</files>
  <action>
    Expand volumetry.spec.ts with tests for remaining modern storage platforms.

    Microsoft S2D tests:
    - Add describe block: "Volumetry Engine - Microsoft S2D"
    - Test topologies: Simple, Mirror (2-way, 3-way), Parity, Dual Parity
    - Validate rebuild reserve: subtract 1 drive equivalent per node
    - Mirror-Accelerated Parity (MAP): hybrid efficiency

    Ceph tests:
    - Add describe block: "Volumetry Engine - Ceph"
    - Replicated pools: efficiency = 1/replica_count (e.g., 3-way = 33.33%)
    - Erasure Coded pools: k+m scheme (e.g., 8+3 = 72.73% efficiency)
    - Property-based test: EC efficiency = k/(k+m)

    Nutanix tests:
    - Add describe block: "Volumetry Engine - Nutanix"
    - RF2 (Replication Factor 2): 50% efficiency (mirroring)
    - RF3 (Replication Factor 3): 33.33% efficiency
    - Erasure Coding: configurable k+m schemes

    For each platform:
    - Create inline test vectors (no separate fixture file needed)
    - Validate efficiency calculations match vendor formulas
    - Use table-driven approach with test.each for multiple configurations
    - Document vendor documentation sources in test comments

    Use research pattern for inline vectors:
    ```typescript
    const s2dVectors = [
      { name: 'S2D Mirror 2-way', config: {...}, expectedEfficiency: 0.50 },
      // ... more vectors
    ]
    describe.each(s2dVectors)('$name', ({ config, expectedEfficiency }) => {
      // test logic
    })
    ```
  </action>
  <verify>
    npm test -- volumetry (all new tests pass)
    grep -c "S2D\\|Ceph\\|Nutanix" tests/engines/volumetry.spec.ts (at least 15 occurrences)
    npm run test:coverage -- volumetry (volumetry engine coverage increases significantly)
  </verify>
  <done>
    - S2D topology tests added (Simple, Mirror, Parity, MAP)
    - Ceph topology tests added (Replicated, Erasure Coded)
    - Nutanix topology tests added (RF2, RF3, EC)
    - All modern storage platforms validated against vendor formulas
    - Volumetry engine coverage significantly increased
    - All tests passing with no failures
  </done>
</task>

</tasks>

<verification>
Run full test suite with coverage:
```bash
npm run test:coverage -- volumetry
```

Expected results:
- Volumetry tests: 80+ tests passing (adding ~30 from this plan)
- ZFS topologies covered (Stripe, Mirror, RAID-Z1/Z2/Z3, dRAID)
- vSAN ESA/OSA adaptive efficiency validated
- S2D, Ceph, Nutanix topologies covered
- Volumetry engine coverage approaching or exceeding 75% threshold
- All industry validation requirements (TEST-09, TEST-10, TEST-11) satisfied
</verification>

<success_criteria>
- [ ] zfs-vectors.ts and vsan-vectors.ts created with vendor-validated data
- [ ] ZFS slop factor edge cases tested (min/max bounds)
- [ ] vSAN ESA adaptive efficiency validated (cluster size dependency)
- [ ] S2D rebuild reserve logic tested
- [ ] Ceph and Nutanix efficiency formulas validated
- [ ] Volumetry engine has comprehensive topology coverage
- [ ] All new tests passing
- [ ] Coverage for volumetry engine approaches 75% threshold
</success_criteria>

<output>
After completion, create `.planning/phases/02-calculation-validation/02-02-SUMMARY.md` following summary template.
</output>
