---
phase: 02-calculation-validation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/engines/volumetry.spec.ts
  - tests/fixtures/raid-vectors.ts
  - package.json
  - package-lock.json
autonomous: true

must_haves:
  truths:
    - "RAID 0 through RAID 60 capacity calculations match WintelGuy within 1% tolerance"
    - "Table-driven tests systematically cover all 11 standard RAID levels"
    - "Property-based tests validate RAID mathematical invariants across random inputs"
  artifacts:
    - path: "tests/fixtures/raid-vectors.ts"
      provides: "WintelGuy-validated test vectors for all RAID levels"
      min_lines: 100
    - path: "tests/engines/volumetry.spec.ts"
      provides: "Expanded RAID tests using table-driven and property-based approaches"
      contains: "describe.each.*raidVectors"
      min_tests: 50
    - path: "package.json"
      provides: "fast-check dependency for property-based testing"
      contains: "fast-check"
  key_links:
    - from: "tests/engines/volumetry.spec.ts"
      to: "tests/fixtures/raid-vectors.ts"
      via: "import statement"
      pattern: "import.*raid-vectors"
    - from: "tests/engines/volumetry.spec.ts"
      to: "@/engines/volumetry"
      via: "calculateVolumetry function calls"
      pattern: "calculateVolumetry\\("
    - from: "property-based tests"
      to: "fast-check"
      via: "fc.assert calls"
      pattern: "fc\\.assert"
---

<objective>
Validate standard RAID capacity calculations (levels 0/1/1E/3/4/5/5E/5EE/6/10/50/60) against industry reference WintelGuy calculator within 1% tolerance using table-driven and property-based testing approaches.

Purpose: RAID calculations are the foundation of the volumetry engine. If these formulas are incorrect, users will make wrong storage capacity decisions. This plan establishes baseline accuracy validation.

Output: Comprehensive RAID test suite with WintelGuy-validated test vectors and property-based invariant testing.
</objective>

<execution_context>
@/Users/fjacquet/Projects/raidy/.claude/get-shit-done/workflows/execute-plan.md
@/Users/fjacquet/Projects/raidy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/fjacquet/Projects/raidy/.planning/PROJECT.md
@/Users/fjacquet/Projects/raidy/.planning/ROADMAP.md
@/Users/fjacquet/Projects/raidy/.planning/STATE.md
@/Users/fjacquet/Projects/raidy/.planning/REQUIREMENTS.md
@/Users/fjacquet/Projects/raidy/.planning/phases/02-calculation-validation/02-RESEARCH.md

# Existing test infrastructure
@/Users/fjacquet/Projects/raidy/.planning/phases/01-test-infrastructure/01-02-SUMMARY.md

# Current test file to expand
@/Users/fjacquet/Projects/raidy/tests/engines/volumetry.spec.ts
@/Users/fjacquet/Projects/raidy/src/engines/volumetry/index.ts
@/Users/fjacquet/Projects/raidy/src/types/topology.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install fast-check and create RAID test vectors file</name>
  <files>
    package.json
    package-lock.json
    tests/fixtures/raid-vectors.ts
  </files>
  <action>
    Install fast-check for property-based testing:
    - Run: npm install fast-check --save-dev
    - Verify installation completes without errors

    Create tests/fixtures/raid-vectors.ts with WintelGuy-validated test vectors:
    - Export const standardRAIDVectors array with objects containing:
      - name: string (e.g., "RAID 5: 8x 1TB")
      - level: RaidLevel type
      - drives: number
      - driveSize: number (bytes)
      - expectedUsable: number (bytes)
      - tolerance: number (0.01 for 1%)
      - source: "WintelGuy RAID Calculator"
      - url: "https://wintelguy.com/raidcalc.pl"
    - Include at least 2 test vectors per RAID level (RAID0, RAID1, RAID1E, RAID3, RAID4, RAID5, RAID5E, RAID5EE, RAID6, RAID10, RAID50, RAID60)
    - Use simple round numbers for easy manual verification (1TB drives, 4/8/12 drive counts)
    - Document expected capacity formulas in comments:
      - RAID 0: N drives
      - RAID 1: N/2 drives
      - RAID 5: N-1 drives
      - RAID 6: N-2 drives
      - RAID 10: N/2 drives (mirror then stripe)
      - etc.

    Why fast-check: Property-based testing discovers edge cases that manual test vectors miss. Research shows it's standard for mathematical validation.
  </action>
  <verify>
    npm list fast-check (shows installed version)
    ls -la tests/fixtures/raid-vectors.ts (file exists)
    grep -c "name:" tests/fixtures/raid-vectors.ts (at least 22 vectors = 2 per level × 11 levels)
  </verify>
  <done>
    - fast-check appears in package.json devDependencies
    - tests/fixtures/raid-vectors.ts exists with 22+ test vectors
    - Each vector has name, level, drives, driveSize, expectedUsable, tolerance, source, url
    - Formulas documented in comments
  </done>
</task>

<task type="auto">
  <name>Task 2: Expand RAID 0/1/1E/3/4 tests with table-driven approach</name>
  <files>tests/engines/volumetry.spec.ts</files>
  <action>
    Read existing volumetry.spec.ts and expand the Standard RAID section.

    Add table-driven tests using describe.each for RAID 0/1/1E/3/4:
    - Import standardRAIDVectors from raid-vectors fixture
    - Filter vectors for levels RAID0, RAID1, RAID1E, RAID3, RAID4
    - Use describe.each(filteredVectors) pattern from research
    - Each test should:
      - Create VolumetryInput with specified drives/topology
      - Call calculateVolumetry
      - Assert result.usableCapacity is within tolerance of expectedUsable
      - Use toBeCloseTo matcher with appropriate precision

    Add property-based tests for RAID invariants:
    - Import fc from 'fast-check'
    - RAID 0: capacity increases monotonically with drive count
    - RAID 1: capacity equals N/2 drives (mirrors)
    - RAID 0: adding drives increases capacity by exactly 1 drive worth
    - Use fc.integer for drive counts (min: minimum for level, max: 24)
    - Use fc.integer for drive sizes (min: 100GB, max: 20TB)
    - Set numRuns: 50 for balance between coverage and speed

    Keep existing describe structure, expand with new tests.
    Do NOT remove existing tests - only add to them.
  </action>
  <verify>
    npm test -- volumetry (tests pass)
    grep -c "describe.each.*raidVectors" tests/engines/volumetry.spec.ts (at least 1)
    grep -c "fc.assert" tests/engines/volumetry.spec.ts (at least 3 property tests)
  </verify>
  <done>
    - Table-driven tests added for RAID 0/1/1E/3/4 using describe.each
    - Property-based tests validate monotonicity and mathematical invariants
    - All new tests pass when running npm test
    - Test file imports raid-vectors fixture and fast-check
  </done>
</task>

<task type="auto">
  <name>Task 3: Expand RAID 5/5E/5EE/6/10/50/60 tests with WintelGuy validation</name>
  <files>tests/engines/volumetry.spec.ts</files>
  <action>
    Continue expanding volumetry.spec.ts with remaining RAID levels.

    Add table-driven tests for RAID 5/5E/5EE/6/10/50/60:
    - Filter standardRAIDVectors for these levels
    - Use same describe.each pattern as Task 2
    - Validate against WintelGuy expected values within 1% tolerance
    - Special attention to:
      - RAID 5E/5EE (distributed hot spare logic)
      - RAID 50/60 (nested RAID validation)

    Add property-based tests for parity RAID:
    - RAID 5: N-1 drives usable, adding drives increases capacity
    - RAID 6: N-2 drives usable, survives 2 failures
    - RAID 10: exactly 50% efficiency (mirrors)
    - RAID 50/60: efficiency combines striping + parity

    Add write penalty validation (connects to TEST-12):
    - RAID 5 write penalty = 4x (read-modify-write for parity)
    - RAID 6 write penalty = 6x (double parity update)
    - Document formulas in test comments with source links

    Document test vector sources:
    - Add comments with WintelGuy calculator URLs
    - Note tolerance reasoning (1% accounts for filesystem overhead variations)
  </action>
  <verify>
    npm test -- volumetry (all tests pass)
    grep -c "RAID.*5E\\|5EE\\|50\\|60" tests/engines/volumetry.spec.ts (at least 8 occurrences)
    npm run test:coverage -- volumetry (volumetry engine coverage increases)
  </verify>
  <done>
    - All 11 standard RAID levels have table-driven tests with WintelGuy validation
    - Property-based tests validate parity RAID mathematical properties
    - Write penalty formulas documented with industry source links
    - Running npm test shows increased test count and passing results
    - Coverage for volumetry engine increases toward 75% threshold
  </done>
</task>

</tasks>

<verification>
Run full test suite with coverage:
```bash
npm run test:coverage
```

Expected results:
- Volumetry tests: 50+ tests passing (up from ~15)
- RAID levels 0/1/1E/3/4/5/5E/5EE/6/10/50/60 all covered
- fast-check property tests executing successfully
- Coverage for src/engines/volumetry increasing (partial - Plan 02 adds ZFS/advanced)
- No test failures or TypeScript errors
</verification>

<success_criteria>
- [ ] fast-check installed and appears in package.json
- [ ] tests/fixtures/raid-vectors.ts exists with 22+ WintelGuy-validated vectors
- [ ] tests/engines/volumetry.spec.ts expanded with table-driven tests using describe.each
- [ ] Property-based tests validate RAID invariants using fc.assert
- [ ] All 11 standard RAID levels have test coverage
- [ ] Tests validate capacity calculations within 1% of WintelGuy
- [ ] Test suite passes with no failures
- [ ] Coverage increase visible in coverage report
</success_criteria>

<output>
After completion, create `.planning/phases/02-calculation-validation/02-01-SUMMARY.md` following summary template.
</output>
