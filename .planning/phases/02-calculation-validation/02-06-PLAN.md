---
phase: 02-calculation-validation
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/engines/performance.spec.ts
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "PowerFlex, ObjectScale, PowerStore, PowerScale, and Nutanix latency calculations are tested"
    - "Advanced topology IOPS calculations include compression, dedup, and network type variations"
    - "Performance engine test coverage reaches 75% threshold"
  artifacts:
    - path: "tests/engines/performance.spec.ts"
      provides: "Advanced topology performance tests (PowerFlex, ObjectScale, PowerStore, PowerScale, Nutanix)"
      contains: "powerflex|objectscale|powerstore|powerscale|nutanix.*compression"
      min_tests: 85
  key_links:
    - from: "Advanced topology tests"
      to: "src/engines/performance/index.ts lines 488-504, 513-528"
      via: "Test coverage for uncovered latency calculations"
      pattern: "calculatePerformance.*powerflex|nutanix.*networkType"
---

<objective>
Add performance tests for advanced storage topologies (PowerFlex, ObjectScale, PowerStore, PowerScale, Nutanix) to close coverage gap from 50.70% to 75%+.

Purpose: Lines 489-504 and 513-528 in performance engine contain untested latency calculations for Dell/Nutanix platforms. These calculations affect IOPS predictions for enterprise storage systems.

Output: Comprehensive performance tests for all advanced topologies, closing the 24.3 percentage point coverage gap.
</objective>

<execution_context>
@/Users/fjacquet/Projects/raidy/.claude/get-shit-done/workflows/execute-plan.md
@/Users/fjacquet/Projects/raidy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/fjacquet/Projects/raidy/.planning/PROJECT.md
@/Users/fjacquet/Projects/raidy/.planning/ROADMAP.md
@/Users/fjacquet/Projects/raidy/.planning/REQUIREMENTS.md
@/Users/fjacquet/Projects/raidy/.planning/phases/02-calculation-validation/02-VERIFICATION.md

# Existing performance tests
@/Users/fjacquet/Projects/raidy/tests/engines/performance.spec.ts

# Performance engine implementation (uncovered lines)
@/Users/fjacquet/Projects/raidy/src/engines/performance/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add PowerFlex and ObjectScale performance tests</name>
  <files>tests/engines/performance.spec.ts</files>
  <action>
    Expand performance.spec.ts with tests for Dell PowerFlex and ObjectScale topologies.

    Add describe block: "Performance Engine - Dell PowerFlex Performance"

    PowerFlex latency tests (covers lines 488-489):
    - Test PowerFlex latency calculation: mediaLatency * 1.5 + networkLatency + cpuOverhead
    - Validate compression overhead when enabled
    - Test 2-way mirror vs 3-way mirror IOPS differences
    - Validate PowerFlex dynamic rebuild performance impact
    - Test scenarios:
      - PowerFlex 2-way mirror with 12x NVMe
      - PowerFlex 3-way mirror with 24x SSD
      - PowerFlex with compression enabled vs disabled

    Add describe block: "Performance Engine - Dell ObjectScale Performance"

    ObjectScale latency tests (covers lines 491-495):
    - Test ObjectScale S3 latency: mediaLatency * 2 + networkLatency * 1.5 + cpuOverhead
    - Validate erasure coding CPU overhead (CPU_OVERHEAD_US.erasure_coding)
    - Test eventual consistency impact on performance
    - Validate higher protocol overhead (networkLatency * 1.5)
    - Test scenarios:
      - ObjectScale EC 12+4 with HDD
      - ObjectScale EC 8+2 with SSD
      - ObjectScale S3 protocol overhead validation

    Use existing test patterns from the file:
    - Create PerformanceInput with appropriate topology options
    - Call calculatePerformance
    - Validate latency calculations against expected formulas
    - Use toBeCloseTo for floating-point comparisons

    Reference lines 488-495 in src/engines/performance/index.ts for exact formulas.
  </action>
  <verify>
    npm test -- performance (all PowerFlex/ObjectScale tests pass)
    grep -c "powerflex\\|objectscale" tests/engines/performance.spec.ts (at least 8 occurrences)
  </verify>
  <done>
    - PowerFlex performance tests added (latency, compression overhead)
    - ObjectScale performance tests added (S3 protocol overhead, EC CPU overhead)
    - Lines 488-495 in performance engine now covered
    - All new tests passing
  </done>
</task>

<task type="auto">
  <name>Task 2: Add PowerStore and PowerScale performance tests</name>
  <files>tests/engines/performance.spec.ts</files>
  <action>
    Continue expanding performance.spec.ts with Dell PowerStore and PowerScale topologies.

    Add describe block: "Performance Engine - Dell PowerStore Performance"

    PowerStore latency tests (covers lines 497-499):
    - Test PowerStore latency: mediaLatency * 1.2 + cpuOverhead
    - Validate NVMe optimization (lower overhead than other platforms)
    - Test PowerStore block storage performance characteristics
    - Test scenarios:
      - PowerStore with 24x NVMe drives
      - PowerStore vs PowerFlex latency comparison (PowerStore should be faster)
      - Validate 1.2x multiplier is applied correctly

    Add describe block: "Performance Engine - Dell PowerScale Performance"

    PowerScale latency tests (covers lines 501-504):
    - Test PowerScale latency: mediaLatency * 1.5 + networkLatency + cpuOverhead
    - Validate replication CPU overhead (CPU_OVERHEAD_US.replication)
    - Test scale-out NAS parity write performance
    - Validate network latency component for NAS protocol
    - Test scenarios:
      - PowerScale with 36x HDD (scale-out NAS)
      - PowerScale with parity writes (1.5x multiplier)
      - PowerScale network latency impact

    Use consistent test structure:
    - Create input with DEFAULT_POWERFLEX_OPTIONS or similar
    - Validate latency components (media, network, CPU)
    - Compare against expected formula results
    - Test with different drive types (HDD, SSD, NVMe)

    Reference lines 497-504 in src/engines/performance/index.ts for formulas.
  </action>
  <verify>
    npm test -- performance (all PowerStore/PowerScale tests pass)
    grep -c "powerstore\\|powerscale" tests/engines/performance.spec.ts (at least 6 occurrences)
  </verify>
  <done>
    - PowerStore performance tests added (NVMe optimization)
    - PowerScale performance tests added (scale-out NAS, parity writes)
    - Lines 497-504 in performance engine now covered
    - All new tests passing
  </done>
</task>

<task type="auto">
  <name>Task 3: Add Nutanix performance tests with compression/dedup/network variations</name>
  <files>tests/engines/performance.spec.ts</files>
  <action>
    Expand performance.spec.ts with comprehensive Nutanix DSF performance tests.

    Add describe block: "Performance Engine - Nutanix DSF Performance"

    Nutanix latency tests (covers lines 510-528):
    - Base latency formula: mediaLatency * 2 + nutanixNetworkLatency + cpuOverhead
    - Test OpLog write pattern (2x media latency for OpLog + destage)
    - Validate CVM-to-CVM replication overhead

    Network type variations (lines 521-526):
    - RDMA: 100μs network latency (0.1ms)
    - 25GbE: 250μs network latency (0.25ms)
    - 10GbE: 500μs network latency (0.5ms)
    - Test all three network types with same drive configuration
    - Validate RDMA is fastest, 10GbE is slowest

    Compression/dedup variations (lines 514-519):
    - Test compression disabled: only replication CPU overhead
    - Test compression enabled: replication + compression CPU overhead
    - Test dedup enabled: replication + dedup CPU overhead
    - Test compression + dedup: replication + compression + dedup overhead
    - Validate CPU overhead accumulates correctly

    Test scenarios:
    - Nutanix RF2 with RDMA, no compression/dedup (minimal overhead)
    - Nutanix RF3 with 10GbE, compression + dedup (maximum overhead)
    - Nutanix with 25GbE, compression only (medium overhead)
    - Compare latency across all combinations

    Property-based tests:
    - RDMA always faster than 25GbE, which is faster than 10GbE
    - Compression/dedup increases CPU overhead
    - OpLog pattern causes 2x media latency consistently

    Use DEFAULT_NUTANIX_OPTIONS from @/types and modify networkType, compression, dedup fields.

    Reference lines 510-528 in src/engines/performance/index.ts for exact logic.
  </action>
  <verify>
    npm test -- performance (all Nutanix tests pass)
    grep -c "nutanix.*rdma\\|nutanix.*compression\\|nutanix.*dedup" tests/engines/performance.spec.ts (at least 10 occurrences)
    npm run test:coverage -- performance (performance engine coverage reaches 75%+)
  </verify>
  <done>
    - Nutanix DSF performance tests added
    - Network type variations tested (RDMA, 25GbE, 10GbE)
    - Compression and dedup overhead variations tested
    - Lines 510-528 in performance engine now covered
    - Performance engine coverage reaches 75%+ threshold
    - All tests passing with no failures
  </done>
</task>

</tasks>

<verification>
Run full test suite with coverage:
```bash
npm run test:coverage -- performance
```

Expected results:
- Performance tests: 85+ tests passing (adding ~7-10 new tests)
- PowerFlex latency calculations tested (lines 488-489 covered)
- ObjectScale S3 protocol overhead tested (lines 491-495 covered)
- PowerStore NVMe optimization tested (lines 497-499 covered)
- PowerScale scale-out NAS tested (lines 501-504 covered)
- Nutanix DSF with all variations tested (lines 510-528 covered)
- Performance engine coverage: 50.70% -> 75%+ (24.3+ point increase)
- Gap closed: Advanced topology IOPS calculations fully validated
</verification>

<success_criteria>
- [ ] PowerFlex performance tests added and passing
- [ ] ObjectScale S3 performance tests added and passing
- [ ] PowerStore NVMe optimization tests added and passing
- [ ] PowerScale scale-out NAS tests added and passing
- [ ] Nutanix DSF tests added with network type variations (RDMA, 25GbE, 10GbE)
- [ ] Nutanix compression and dedup overhead variations tested
- [ ] All uncovered lines 488-504, 510-528 now have test coverage
- [ ] Performance engine coverage reaches 75%+ threshold
- [ ] All new tests passing with no failures
- [ ] Gap 1 from VERIFICATION.md closed (advanced topology performance coverage)
</success_criteria>

<output>
After completion, create `.planning/phases/02-calculation-validation/02-06-SUMMARY.md` following summary template.
</output>
