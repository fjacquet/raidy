---
phase: 14-dependency-maintenance
plan: 03
type: execute
wave: 3
depends_on:
  - 14-02
files_modified:
  - package.json
  - package-lock.json
  - src/i18n/locales/en/common.json
  - src/i18n/locales/fr/common.json
  - src/i18n/locales/de/common.json
  - src/i18n/locales/it/common.json
autonomous: false
requirements:
  - DEPS-01

must_haves:
  truths:
    - "i18next is at 25.10.10 in package-lock.json (NOT 26.x)"
    - "react-i18next is at 16.6.6 in package-lock.json (NOT 17.x)"
    - "@vitejs/plugin-react is at 5.2.0 in package-lock.json (NOT 6.x)"
    - "All four language translations render correctly in the browser"
    - "npm run build exits 0 with no TypeScript or Vite errors"
  artifacts:
    - path: "package.json"
      provides: "Updated specifiers for i18next, react-i18next, @vitejs/plugin-react"
      contains: "react-i18next"
    - path: "package-lock.json"
      provides: "Resolved dependency tree with i18n and build tool updates"
  key_links:
    - from: "src/i18n/i18n.ts"
      to: "i18next"
      via: "i18next.init()"
      pattern: "i18next\\.init|createInstance"
    - from: "src/main.tsx"
      to: "react-i18next"
      via: "I18nextProvider"
      pattern: "I18nextProvider|initReactI18next"
    - from: "vite.config.ts"
      to: "@vitejs/plugin-react"
      via: "plugins: [react()]"
      pattern: "react\\(\\)"
---

<objective>
Update the i18n stack (i18next 25.8 → 25.10, react-i18next 16.5 → 16.6) and the Vite React plugin (@vitejs/plugin-react 5.1 → 5.2) to their latest compatible minor versions, then perform a full build and browser smoke-test.

Purpose: These three packages have the highest coupling to application code in this phase — i18next and react-i18next are wired into every component, and @vitejs/plugin-react transforms all JSX. A dedicated plan isolates any breaking changes and ends with a human-verified browser check before the phase is declared complete.
Output: Updated package.json/lock, confirmed build, human-verified browser render of all four languages.
</objective>

<execution_context>
@/Users/fjacquet/.claude/get-shit-done/workflows/execute-plan.md
@/Users/fjacquet/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-dependency-maintenance/14-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update i18next and react-i18next to latest minor versions</name>
  <files>package.json, package-lock.json</files>
  <action>
    Run:

    ```
    npm install i18next@25.10.10 react-i18next@16.6.6
    ```

    IMPORTANT version guards:
    - Install i18next@25.10.10 explicitly — do NOT allow npm to resolve to 26.x (major, breaking)
    - Install react-i18next@16.6.6 explicitly — do NOT allow npm to resolve to 17.x (major, breaking)

    After install, run tests to verify i18n integration still works:

    ```
    npm test -- --run
    ```

    If tests fail, look for these known minor-version migration patterns:
    - i18next 25.9/25.10 may have changed how `interpolation.escapeValue` defaults behave — check i18next CHANGELOG for the 25.8 → 25.10 range
    - react-i18next 16.6 may have tightened TypeScript types for `useTranslation` — fix type errors in call sites, not by widening types
    - If a `t()` call with a namespace prefix (e.g. `t('topology:level.raid5.description')`) breaks, check that namespace separator config is unchanged

    Do NOT upgrade to i18next@26 or react-i18next@17 under any circumstances — those are separate major migrations outside this phase scope.
  </action>
  <verify>
    <automated>node -e "const p=JSON.parse(require('fs').readFileSync('package-lock.json','utf8')); const get=n=>p.packages?.['node_modules/'+n]?.version; [['i18next','25.10.10'],['react-i18next','16.6.6']].forEach(([n,v])=>{const got=get(n);if(!got?.startsWith(v))throw new Error(n+' not at '+v+', got '+got);console.log('OK:',n,got)})" && npm test -- --run</automated>
  </verify>
  <done>i18next at 25.10.10 and react-i18next at 16.6.6 in package-lock.json; test suite exits 0</done>
</task>

<task type="auto">
  <name>Task 2: Update @vitejs/plugin-react to 5.2.0 and verify production build</name>
  <files>package.json, package-lock.json</files>
  <action>
    Run:

    ```
    npm install --save-dev "@vitejs/plugin-react@5.2.0"
    ```

    IMPORTANT: Install 5.2.0 explicitly — do NOT allow npm to resolve to 6.x (major, breaking).

    Then run the full production build to confirm Vite + React transform still works:

    ```
    npm run build
    ```

    If the build fails with a Vite/React transform error, check whether @vitejs/plugin-react 5.2.0 requires a minimum Vite version above the current 7.x — consult the plugin's CHANGELOG. If a config change is needed in vite.config.ts, make the minimum change required.

    Also run typecheck to catch any new type errors introduced by the plugin update:

    ```
    npm run typecheck
    ```
  </action>
  <verify>
    <automated>node -e "const p=JSON.parse(require('fs').readFileSync('package-lock.json','utf8')); const v=p.packages?.['node_modules/@vitejs/plugin-react']?.version; if(!v?.startsWith('5.2.0'))throw new Error('@vitejs/plugin-react not at 5.2.0, got '+v); console.log('OK: @vitejs/plugin-react',v)" && npm run build && npm run typecheck</automated>
  </verify>
  <done>@vitejs/plugin-react at 5.2.0; `npm run build` and `npm run typecheck` both exit 0</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    All npm packages updated to their latest compatible (wanted) versions. Full CI suite
    (typecheck + lint + tests + build) passes. The app has been rebuilt from source.
  </what-built>
  <how-to-verify>
    1. Run `npm run dev` to start the development server
    2. Open http://localhost:5173/raidy/ in your browser
    3. Verify the app loads without console errors (open DevTools → Console)
    4. Switch language using the language selector — test EN, FR, DE, IT in turn
    5. Confirm all UI labels render in the correct language (no missing translation keys shown as raw key strings like `topology:level.raid5.description`)
    6. Change a topology type (e.g. RAID → ZFS) and confirm the output dashboard updates correctly
    7. Confirm the Sankey diagram, speedometer gauge, and donut chart all render without errors
    8. Run `npm outdated` and confirm the only remaining entries are packages intentionally held back (jsdom, typescript, vite — already at wanted, major skip) and i18next/react-i18next (already at wanted, major 26/17 skip)
  </how-to-verify>
  <resume-signal>Type "approved" if everything looks correct, or describe any issues observed</resume-signal>
</task>

</tasks>

<verification>
After all tasks:
- `npm list i18next react-i18next @vitejs/plugin-react` shows 25.10.10 / 16.6.6 / 5.2.0
- `npm run build` exits 0
- `npm run typecheck` exits 0
- `npm run lint` exits 0
- `npm test -- --run` exits 0
- `npm outdated` shows no packages with available updates within current semver constraints
- Browser smoke-test passes (all 4 languages render, no console errors)
</verification>

<success_criteria>
- i18next at 25.10.10, react-i18next at 16.6.6, @vitejs/plugin-react at 5.2.0 in package-lock.json
- Production build succeeds
- All CI checks pass
- `npm outdated` shows only intentionally-held-back majors (jsdom 29, typescript 6, vite 8, i18next 26, react-i18next 17, @vitejs/plugin-react 6)
- Human-verified browser render confirms no runtime regressions
</success_criteria>

<output>
After completion, create `.planning/phases/14-dependency-maintenance/14-03-SUMMARY.md`
</output>
