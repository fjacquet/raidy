---
phase: 14-dependency-maintenance
plan: 02
type: execute
wave: 2
depends_on:
  - 14-01
files_modified:
  - package.json
  - package-lock.json
autonomous: true
requirements:
  - DEPS-01

must_haves:
  truths:
    - "@biomejs/biome is at 2.4.10 in package-lock.json"
    - "vitest, @vitest/coverage-v8, and @vitest/ui are all at 4.1.2 in package-lock.json"
    - "npm run lint exits 0 after any auto-fixable biome rule changes are applied"
    - "npm test -- --run exits 0 after vitest upgrade"
  artifacts:
    - path: "package.json"
      provides: "Updated version specifiers for biome and the vitest suite"
      contains: "@biomejs/biome"
    - path: "package-lock.json"
      provides: "Resolved dependency tree after tool chain updates"
  key_links:
    - from: "package.json"
      to: "package-lock.json"
      via: "npm install"
      pattern: "\"@biomejs/biome\": \"2\\.4\\.10\""
    - from: "vitest config"
      to: "@vitest/coverage-v8"
      via: "vitest run --coverage"
      pattern: "coverage"
---

<objective>
Update the linting and testing tool chain: @biomejs/biome 2.4.6 → 2.4.10 and the vitest suite (vitest + @vitest/coverage-v8 + @vitest/ui) 4.0.18 → 4.1.2.

Purpose: These tool updates carry slightly higher risk than pure patch library bumps — biome minor releases may add new lint rules that flag existing code, and vitest minor releases may change test runner behaviour or config options. Isolating them in their own plan makes failures easy to diagnose and revert.
Output: Updated package.json and package-lock.json; any biome-flagged source fixes applied; full test suite green.
</objective>

<execution_context>
@/Users/fjacquet/.claude/get-shit-done/workflows/execute-plan.md
@/Users/fjacquet/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-dependency-maintenance/14-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update @biomejs/biome to 2.4.10</name>
  <files>package.json, package-lock.json</files>
  <action>
    Run:

    ```
    npm install --save-dev @biomejs/biome@2.4.10
    ```

    Then immediately run lint to detect any newly enabled rules:

    ```
    npm run lint
    ```

    If lint fails, run:

    ```
    npm run lint:fix
    ```

    Inspect every auto-fix diff. Accept fixes that are purely stylistic (import order, trailing commas, quote style). If lint:fix introduces a logic-changing auto-fix in a source file, revert that specific file with `git checkout -- <file>` and add a biome ignore comment instead, noting the rule name.

    Do NOT modify `biome.json` to disable rules — fix the code.
  </action>
  <verify>
    <automated>node -e "const p=JSON.parse(require('fs').readFileSync('package-lock.json','utf8')); const v=p.packages?.['node_modules/@biomejs/biome']?.version; if(!v?.startsWith('2.4.10'))throw new Error('@biomejs/biome not at 2.4.10, got '+v); console.log('OK: @biomejs/biome',v)" && npm run lint</automated>
  </verify>
  <done>@biomejs/biome resolved to 2.4.10 in package-lock.json and `npm run lint` exits 0</done>
</task>

<task type="auto">
  <name>Task 2: Update vitest suite to 4.1.2</name>
  <files>package.json, package-lock.json</files>
  <action>
    Update all three vitest packages together — they must be on the same version to avoid internal version mismatches:

    ```
    npm install --save-dev vitest@4.1.2 @vitest/coverage-v8@4.1.2 "@vitest/ui@4.1.2"
    ```

    Then run the full test suite:

    ```
    npm test -- --run
    ```

    Expected: all tests pass. If any test fails, check the vitest 4.0 → 4.1 changelog for:
    - Changed default reporter output (cosmetic, ignore)
    - Removed or renamed config options in vitest.config.ts (fix the config)
    - Changed behaviour of `vi.mock` or `vi.spyOn` (investigate per failing test)
    - fast-check integration changes (unlikely — fast-check was updated in plan 01)

    Do NOT skip or comment out failing tests — understand and fix the root cause.

    Also run coverage to confirm the v8 provider is still wired correctly:

    ```
    npm run test:coverage
    ```
  </action>
  <verify>
    <automated>node -e "const p=JSON.parse(require('fs').readFileSync('package-lock.json','utf8')); const get=n=>p.packages?.['node_modules/'+n]?.version; ['vitest','@vitest/coverage-v8','@vitest/ui'].forEach(n=>{const v=get(n);if(!v?.startsWith('4.1.2'))throw new Error(n+' not at 4.1.2, got '+v);console.log('OK:',n,v)})" && npm test -- --run</automated>
  </verify>
  <done>All three vitest packages at 4.1.2 in package-lock.json; `npm test -- --run` exits 0 with no failing tests</done>
</task>

</tasks>

<verification>
After all tasks:
- `npm list @biomejs/biome vitest @vitest/coverage-v8 @vitest/ui` shows 2.4.10 / 4.1.2 / 4.1.2 / 4.1.2
- `npm run lint` exits 0
- `npm test -- --run` exits 0
- `npm run test:coverage` exits 0 (coverage thresholds met)
</verification>

<success_criteria>
- @biomejs/biome resolved to 2.4.10 with no lint errors in source files
- vitest, @vitest/coverage-v8, @vitest/ui all resolved to 4.1.2
- Test suite fully green; coverage thresholds (75% on engines/workers/utils) still met
</success_criteria>

<output>
After completion, create `.planning/phases/14-dependency-maintenance/14-02-SUMMARY.md`
</output>
