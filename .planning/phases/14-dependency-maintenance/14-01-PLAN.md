---
phase: 14-dependency-maintenance
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
autonomous: true
requirements:
  - DEPS-01

must_haves:
  truths:
    - "npm install completes with no peer conflict errors"
    - "dompurify is at 3.3.3, zustand is at 5.0.12, recharts is at 3.8.1"
    - "snyk, fast-check, @types/node, tailwindcss, @tailwindcss/vite are each at their latest wanted version"
  artifacts:
    - path: "package.json"
      provides: "Updated dependency version specifiers for low-risk patch packages"
      contains: "dompurify"
    - path: "package-lock.json"
      provides: "Resolved dependency tree after low-risk patch updates"
  key_links:
    - from: "package.json"
      to: "package-lock.json"
      via: "npm install"
      pattern: "\"dompurify\": \"3\\.3\\.3\""
---

<objective>
Update nine low-risk npm packages to their latest compatible versions in a single batch.

Purpose: These are all patch or minor releases with no expected API changes. Updating them together keeps the lock-file churn in one commit and leaves the higher-risk tool chain (biome, vitest suite, i18n) for later plans where breakage is more likely.
Output: Updated package.json and package-lock.json with the nine packages resolved to their wanted versions.
</objective>

<execution_context>
@/Users/fjacquet/.claude/get-shit-done/workflows/execute-plan.md
@/Users/fjacquet/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update low-risk production dependencies</name>
  <files>package.json, package-lock.json</files>
  <action>
    Run one install command to update all low-risk production dependencies to their latest wanted versions:

    ```
    npm install dompurify@3.3.3 recharts@3.8.1 zustand@5.0.12
    ```

    These are all patch/minor bumps within the current semver range:
    - dompurify 3.3.2 → 3.3.3 (patch, security-sensitive, keep current)
    - recharts 3.7.0 → 3.8.1 (minor, UI chart library, no breaking API)
    - zustand 5.0.11 → 5.0.12 (patch, state management)

    Do NOT update i18next, react-i18next, vitest, biome, or any build-tooling package in this task.
  </action>
  <verify>
    <automated>node -e "const p=JSON.parse(require('fs').readFileSync('package-lock.json','utf8')); const get=n=>p.packages?.['node_modules/'+n]?.version; const checks=[['dompurify','3.3.3'],['recharts','3.8.1'],['zustand','5.0.12']]; checks.forEach(([n,v])=>{if(!get(n)?.startsWith(v))throw new Error(n+' not at '+v+', got '+get(n));console.log('OK:',n,get(n))})"</automated>
  </verify>
  <done>package-lock.json shows dompurify 3.3.3, recharts 3.8.1, zustand 5.0.12 — npm install exits 0</done>
</task>

<task type="auto">
  <name>Task 2: Update low-risk devDependencies</name>
  <files>package.json, package-lock.json</files>
  <action>
    Run one install command to update all low-risk dev dependencies:

    ```
    npm install --save-dev fast-check@4.6.0 snyk@1.1303.2 @types/node@25.5.0 tailwindcss@4.2.2 "@tailwindcss/vite@4.2.2"
    ```

    These are all patch/minor bumps:
    - fast-check 4.5.3 → 4.6.0 (minor, property-based test library)
    - snyk 1.1303.1 → 1.1303.2 (patch, security scanner)
    - @types/node 25.3.3 → 25.5.0 (minor, type definitions only)
    - tailwindcss 4.2.1 → 4.2.2 (patch, CSS framework)
    - @tailwindcss/vite 4.2.1 → 4.2.2 (patch, Vite plugin for Tailwind — must match tailwindcss version)

    Do NOT update @vitejs/plugin-react, vitest, or biome in this task.
  </action>
  <verify>
    <automated>node -e "const p=JSON.parse(require('fs').readFileSync('package-lock.json','utf8')); const get=n=>p.packages?.['node_modules/'+n]?.version; const checks=[['fast-check','4.6.0'],['snyk','1.1303.2'],['@types/node','25.5.0'],['tailwindcss','4.2.2'],['@tailwindcss/vite','4.2.2']]; checks.forEach(([n,v])=>{if(!get(n)?.startsWith(v))throw new Error(n+' not at '+v+', got '+get(n));console.log('OK:',n,get(n))})"</automated>
  </verify>
  <done>package-lock.json shows all five devDependencies at their wanted versions — npm install exits 0</done>
</task>

<task type="auto">
  <name>Task 3: Verify baseline CI still passes after patch updates</name>
  <files>N/A</files>
  <action>
    Run the full CI check suite to confirm the patch updates introduce no regressions:

    ```
    npm run typecheck && npm run lint && npm test -- --run
    ```

    If lint fails with new Biome rule violations triggered by the updated packages (not by source code changes), run `npm run lint:fix` and inspect what changed. Do NOT suppress lint errors by modifying biome.json — fix the underlying code issue.

    If tests fail, read the error output carefully. fast-check 4.6.0 may have changed a minor API — check the fast-check changelog for 4.5.x → 4.6.0 if property-based tests break.
  </action>
  <verify>
    <automated>npm run typecheck && npm run lint && npm test -- --run</automated>
  </verify>
  <done>All three CI commands exit 0 with no errors</done>
</task>

</tasks>

<verification>
After all tasks:
- `npm list dompurify recharts zustand fast-check snyk tailwindcss` shows expected versions
- `npm run typecheck` exits 0
- `npm run lint` exits 0
- `npm test -- --run` exits 0 with no failing tests
</verification>

<success_criteria>
- All nine packages (dompurify, recharts, zustand, fast-check, snyk, @types/node, tailwindcss, @tailwindcss/vite) are at their latest wanted versions in package-lock.json
- CI suite (typecheck + lint + test) passes cleanly
- No peer dependency conflicts reported by npm
</success_criteria>

<output>
After completion, create `.planning/phases/14-dependency-maintenance/14-01-SUMMARY.md`
</output>
